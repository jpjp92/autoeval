# DEV 작업 노트 - 2026-03-12

## 📋 완료된 작업 (누적)

### 평가 파이프라인
- [x] `evaluation_api.py`: 4단계 평가 파이프라인 구현
  - Layer 1-A: SyntaxValidator
  - Layer 1-B: DatasetStats
  - Layer 2: RAGTriadEvaluator (TruLens 통합)
  - Layer 3: QAQualityEvaluator (멀티 프로바이더: OpenAI / Gemini / Claude)
- [x] Layer 2/3 동일 평가 모델 일관성 확보 (프론트 선택 모델 → Layer 2, 3 모두 동일 적용)
- [x] `_extract_score`: CoT 마지막 줄 기준 파싱으로 개선
- [x] 중복 `evaluate_clarity` 함수 제거
- [x] `QAQualityEvaluator` 멀티 프로바이더 지원 (이전엔 OpenAI 전용 하드코딩)

### 모델 설정
- [x] `backend/config/models.py`: 중앙 모델 설정 (`MODEL_CONFIG`)
- [x] `_get_model_id()` → MODEL_CONFIG 기준 실제 API model_id로 변환

### Supabase
- [x] 생성/평가 결과 Supabase 자동 저장
- [x] `linked_evaluation_id` FK로 생성↔평가 연결

### 로깅
- [x] TruLens / alembic / google 불필요 반복 로그 억제
- [x] `trulens.experimental.otel_tracing` 로그도 억제

### 리팩토링 (`evaluation_api.py` 분할) ✅ 완료
- [x] `evaluators/__init__.py`
- [x] `evaluators/job_manager.py` — EvalJobStatus, EvalJob, EvaluationManager
- [x] `evaluators/syntax_validator.py` — SyntaxValidator
- [x] `evaluators/dataset_stats.py` — DatasetStats
- [x] `evaluators/rag_triad.py` — RAGTriadEvaluator + clean_markdown
- [x] `evaluators/qa_quality.py` — QAQualityEvaluator
- [x] `evaluators/pipeline.py` — run_full_evaluation_pipeline + run_evaluation
- [x] `evaluators/recommendations.py` — generate_recommendations
- [x] `evaluation_api.py` 축소 완료: **1,545줄 → ~120줄** (라우트만 유지)
- [x] `uv run python -c "import main"` 정상 확인

### 병렬 평가 처리 (`evaluators/pipeline.py`) ✅ 완료
- [x] Layer 1-A (Syntax): 4 workers 고정 (Render/Railway free tier 메모리 안전)
- [x] Layer 2 (RAG Triad): provider별 최적화
- [x] Layer 3 (Quality): provider별 최적화
- [x] `progress_lock` 도입: 멀티스레드 진행 상황 안전 업데이트
- [x] 인덱스 기반 정렬: 원래 QA 순서 보장

### 병렬 생성 처리 (`generation_api.py`) ✅ 완료
- [x] `run_qa_generation_real()` 문서 루프 → `ThreadPoolExecutor` 병렬화
- [x] `GENERATION_MAX_WORKERS` 프로바이더별 설정 (평가보다 낮게)
- [x] `token_lock` + `progress_lock` 스레드 안전 카운터
- [x] 인덱스 기반 정렬으로 문서 순서 보장

### MAX_WORKERS 최종 설정

#### 평가 (`evaluators/pipeline.py`)

| Layer | Anthropic | Google (Paid) | OpenAI |
|---|---|---|---|
| 1-A Syntax | 4 (고정) | 4 (고정) | 4 (고정) |
| 2 RAG Triad | **3** | **10** | **8** |
| 3 Quality | **3** | **10** | **8** |

#### 생성 (`generation_api.py`)

> 생성은 평가보다 요청당 토큰이 크므로 낮게 설정 (특히 claude-sonnet Output TPM 8K 병목)

| 모델 | Anthropic | Google | OpenAI |
|---|---|---|---|
| workers | **2** | **5** | **5** |

---

---

## 📊 Rate Limit 참고표 (MODEL_CONFIG 기준)

#### Anthropic (Tier 1)

| 모델 키 | model_id | RPM | Input TPM | Output TPM |
|---|---|---|---|---|
| `claude-haiku` | claude-haiku-4-5 | 50 | 50K | 10K |
| `claude-sonnet` | claude-sonnet-4-6 | 50 | 30K | **8K** ← 병목 |

#### OpenAI

| 모델 키 | model_id | RPM | TPM |
|---|---|---|---|
| `gpt-5.1` | gpt-5.1-2025-11-13 | 500 | 500K |
| `gpt-5.2` | gpt-5.2-2025-12-11 | 500 | 500K |

#### Google Gemini (Tier 1 Paid)

| 모델 키 | model_id | RPM | TPM | RPD |
|---|---|---|---|---|
| `gemini-flash` | gemini-2.5-flash | 1,000 | 1M | 10K |
| `gemini-3.1-flash` | gemini-3-flash-preview | 1,000 | 2M | 10K |

---


## 📌 기타 TODO (우선순위 낮음)

- [ ] `PIPELINE_DESIGN.md` Supabase 스키마 섹션 업데이트
- [ ] Evaluation Dashboard 상세 뷰 구현
- [ ] `GET /api/generation/{id}` 엔드포인트 추가
