# 🛠️ 개발 로그 (2026-03-17)

## 🎯 오늘의 목표

- 프론트엔드 Standardization + Hierarchy 통합 페이지 완성
- DB 전체 검토 (메타데이터 필드, 계층 태깅, content 품질 진단)
- Phase 3 QA 생성 파이프라인 설계 및 개선 방향 결정

---

## ✅ 완료된 작업

### 1. 프론트엔드 리팩토링 완성

Standardization과 Hierarchy 탭을 하나의 페이지로 통합.

**변경 파일**

| 파일 | 변경 내용 |
|------|-----------|
| `frontend/src/components/standardization/DataStandardizationPanel.tsx` | 업로드 + Hierarchy 분석 통합 UI 완성 (JSX 전체 재작성) |
| `frontend/src/components/layout/Sidebar.tsx` | `Hierarchy` 탭 제거, `Layers` import 제거 |
| `frontend/src/App.tsx` | `HierarchyConstructionPanel` import·라우트·`getHeaderTitle()` 분기 제거 |

**통합 UX 흐름**
```
파일 선택 → "데이터 규격화 시작"
    → 업로드 완료 시 Hierarchy 카드 자동 노출
        → "AI 분석 실행" 클릭
            → L1 후보 칩 + 태깅 샘플 미리보기 표시 (병렬 실행)
```

**DataStandardizationPanel 핵심 변경사항**
- 구형 `message`/`status`/`previews` state → `uploadMessage`, `uploadedFilename`으로 정리
- `handleUpload` 성공 시 `uploadedFilename` 저장 → Hierarchy 카드 조건부 렌더링 트리거
- `runTagging`: `analyze-tagging-samples` + `apply-granular-tagging` `Promise.all` 병렬 실행
- L1 후보 칩 토글 (`toggleL1`), 태깅 샘플 L1 > L2 > L3 badge 체인 표시

---

### 2. DB 전체 검토

#### 결과 요약

| 항목 | 결과 |
|------|------|
| 총 청크 수 | **12개** (page 4~15) |
| chunk_type | `table`(3) / `list`(6) / `heading`(1) / `body`(1) |
| hierarchy 태깅 | 전 청크 `hierarchy_l1`, `hierarchy_l2` 완료 |
| page 값 | **단수 int** (`page: 4`) — 코드 일부에서 `pages`(복수)로 잘못 참조하는 불일치 존재 |
| type 키 | **`chunk_type`** — 코드 일부에서 `type`으로 잘못 참조하는 불일치 존재 |

#### 코드 레벨 키 불일치 목록

| 잘못된 참조 | 실제 DB 키 | 영향 |
|-------------|------------|------|
| `m.get("pages")` | `page` (int) | 페이지 필터 시 `None` 반환 |
| `m.get("type")` | `chunk_type` | 타입 필터 시 빈 값 반환 |

→ `generation_api.py`는 `hierarchy_l1/l2/l3` 키 **정상** 사용 중이므로 계층 필터는 동작함.

#### L1 계층 분포

| L1 | 청크 수 |
|----|---------|
| 가이드 개요 | 5 |
| 데이터 구축 공정 | 3 |
| 유형별 구축 사례 | 2 |
| 품질오류 및 분석 | 1 |
| (발행처) | 1 |

---

### 3. QA 생성 전 Content 품질 진단

모든 청크 content 전문을 조회하여 문제 유형 분류.

#### 문제 분류

| 유형 | 청크 | 문제 | 발생 원인 |
|------|------|------|-----------|
| **파싱 아티팩트** | [1][2][4][5] | 문장 중간 줄바꿈 (`필수적이지만, 데이터\n중복`) | PDF "dict" 모드가 시각적 줄 경계를 `\n`으로 출력 |
| **파싱 아티팩트** | [4][5][8] | `Ÿ` 특수문자 불릿 | PDF 불릿 폰트 → 유니코드 변환 실패 |
| **짧은 청크** | [3] (57자), [7] (258자) | QA 생성 불가 수준의 context 부족 | 최소 필터 50자만 있고 섹션 분리가 너무 세밀함 |
| **청크 끝 잔해** | [0][2] | 다음 섹션 표 헤더 조각이 붙음 | 연속된 섹션 경계에서 스플리터 overlap 영향 |
| **무의미 청크** | [11] | 발행처/저작권 페이지 | QA 생성 불필요, 임베딩 낭비 |

#### 설계 판단: Ingestion 수정 vs Generation 필터

```
범용적으로 재현되는 파싱/청킹 문제 → Ingestion 수정
Task/정책 의존적 판단 → Generation 필터
```

| 항목 | 처리 레이어 | 근거 |
|------|-------------|------|
| `Ÿ` → `-` 불릿 정규화 | **Ingestion** | 모든 PDF에서 공통 발생 |
| 문장 중간 줄바꿈 스마트 결합 | **Ingestion** | PDF 줄바꿈 ≠ 문단 경계, 범용 문제 |
| 짧은 청크 병합 (`< 200자`) | **Ingestion** | 임베딩 품질·비용 모두 낭비 |
| 발행처/저작권 페이지 스킵 | **Ingestion** | 어떤 QA task에도 무의미 |
| `heading` 청크 단독 제외 | **Generation** | task에 따라 제목 기반 QA도 유효 |
| L1 균형 샘플링 | **Generation** | 배분 정책은 task별 가변 |

---

## 🔧 다음 구현 계획

### [Ingestion] `normalize_text` 강화

```python
# 1. Ÿ (불릿 폰트 아티팩트) 정규화
text = re.sub(r'[ŸŸ]', '-', text)  # 유니코드 변형 모두 처리

# 2. 스마트 줄바꿈 결합 (문장 중간 줄바꿈 → 공백)
# 조건: 이전 줄이 .!?。로 끝나지 않고, 다음 줄이 불릿/표/제목이 아닌 경우
def smart_join_lines(text: str) -> str:
    result, buffer = [], ""
    for line in text.split('\n'):
        stripped = line.strip()
        if not buffer:
            buffer = stripped
        elif (buffer
              and not re.search(r'[.!?。]$', buffer)
              and not re.match(r'^[-•|\[#\d]', stripped)):
            buffer += ' ' + stripped
        else:
            result.append(buffer)
            buffer = stripped
    if buffer:
        result.append(buffer)
    return '\n'.join(result)
```

### [Ingestion] 짧은 청크 병합

스플리터 후 후처리 단계에서, 같은 `section_path` 내 인접 청크가 200자 미만이면 다음 청크와 병합 (최대 `chunk_size`까지).

```python
def merge_short_chunks(chunks: list, min_chars: int = 200, max_chars: int = 1200) -> list:
    merged, buf = [], ""
    for chunk in chunks:
        if not buf:
            buf = chunk
        elif len(buf) + len(chunk) < max_chars and len(buf) < min_chars:
            buf += "\n" + chunk
        else:
            merged.append(buf)
            buf = chunk
    if buf:
        merged.append(buf)
    return merged
```

### [Ingestion] 발행처/저작권 청크 필터

```python
COLOPHON_KEYWORDS = ["발행처:", "발행인:", "저작권", "©", "무단전재", "재배포를 금"]

def is_colophon_chunk(text: str) -> bool:
    return sum(1 for kw in COLOPHON_KEYWORDS if kw in text) >= 2
```

---

## ✅ 추가 완료 작업 (2026-03-17 저녁 — P0 수정)

### 5. P0-3: `config/prompts.py` — 시스템 프롬프트 도메인 범용화

**배경**: 시스템 프롬프트가 "통신사 고객지원 QA 데이터셋 생성 전문가"로 하드코딩되어 현재 문서(AI 데이터 구축 가이드 등)와 도메인 불일치.

**변경 내용**

| 항목 | 기존 | 변경 |
|------|------|------|
| `SYSTEM_PROMPT_KO_V1` 도메인 | 통신사 고객지원 전문가 | 문서 기반 QA 데이터셋 생성 전문가 |
| `SYSTEM_PROMPT_KO_V1` 대상 | "고객이 물어볼 법한" | "독자가 물어볼 법한" |
| `SYSTEM_PROMPT_KO_V1` 컨텍스트 표현 | "웹페이지 내용" | "문서 내용" |
| `SYSTEM_PROMPT_EN_V1` 도메인 | Korea telecom (KT) customer support | document-based QA dataset generation |
| `USER_TEMPLATE_KO/EN_V1` 예시 | USIM, eSIM, 배송료, 개통절차 등 통신사 예시 | 항목, 기준, 처리 절차, 유형 등 범용 예시 |

**유지한 것**: 근거성·관련성·원자성 원칙 텍스트, 8종 의도 유형 정의, 컨텍스트 부족 처리, 한국어 출력 강제, JSON 출력 포맷 모두 동일.

---

### 6. P0-2: `config/supabase_client.py` — `get_doc_chunks_by_filter()` 추가

```python
async def get_doc_chunks_by_filter(
    hierarchy_l1, hierarchy_l2, hierarchy_l3, limit
) -> list:
    """metadata 필터 기반 직접 select — vector similarity 없이 hierarchy 필터만"""
    query = supabase_client.table("doc_chunks").select("id, content, metadata")
    if hierarchy_l1: query = query.eq("metadata->>hierarchy_l1", hierarchy_l1)
    if hierarchy_l2: query = query.eq("metadata->>hierarchy_l2", hierarchy_l2)
    if hierarchy_l3: query = query.eq("metadata->>hierarchy_l3", hierarchy_l3)
    return query.limit(limit).execute().data
```

---

### 7. P0-2: `generation_api.py` — dummy zero vector 제거 → 직접 select 필터

**기존 문제**: `r_query`가 없을 때 `[0.0] * 1536` 더미 벡터로 `match_doc_chunks` RPC 호출 → cosine similarity 결과 불확정.

**변경 후 흐름**:
```python
if query_vector is not None:
    # r_query 있음 → 실제 semantic search
    chunks = await search_doc_chunks(query_embedding=query_vector, match_threshold=0.3, ...)
else:
    # r_query 없음 → metadata 필터만, 직접 select
    chunks = await get_doc_chunks_by_filter(hierarchy_l1=h1, hierarchy_l2=h2, ...)
```

---

### 8. P0-1 재검토 결과 — 수정 불필요 확인

**근거**: `ingestion_api.py:820` 확인
```python
await save_doc_chunk(c["raw_text"], normalized_embedding, chunk_metadata)
```
DB `content` 필드 = `raw_text` (순수 본문). `enriched_text`(prefix 포함)는 임베딩 품질을 위해 임베딩 계산에만 사용되고 DB에 저장되지 않음. `main.py:278`의 `text[:2000]`은 이미 prefix-free 텍스트를 처리하므로 수정 불필요.

---

## ✅ 추가 완료 작업 (2026-03-17 오후)

### 3. 프론트엔드 QA 생성 UI — DB 기반 Hierarchy 선택

**배경**: QA Generation 패널의 hierarchy가 `["Shop", "USIM/eSIM 가입", "선불 USIM 구매/충전"]`으로 하드코딩되어 있어 실제 DB 문서와 무관했음.

**방향 결정**: DB(`doc_chunks` metadata)에서 직접 L1/L2 목록을 조회하는 방식 채택.
- Standardization 완료 데이터를 직접 전달하는 방식보다 독립적이고 재사용 가능
- 여러 문서가 DB에 누적되어도 모두 선택 가능

**변경 파일**

| 파일 | 변경 내용 |
|------|-----------|
| `backend/config/supabase_client.py` | `get_hierarchy_list()` 함수 추가 — `doc_chunks` 전체 metadata 조회 → L1 목록 + L1별 L2 목록 반환 |
| `backend/ingestion_api.py` | `GET /api/ingestion/hierarchy-list` 엔드포인트 추가 |
| `frontend/src/lib/api.ts` | `GenerateRequest`에 `hierarchy_l1/l2/l3` 필드 추가, `getHierarchyList()` 함수 추가 |
| `frontend/src/components/generation/QAGenerationPanel.tsx` | 하드코딩 제거 → 마운트 시 DB 조회, L1/L2 드롭다운 + 선택 요약 UI, `generateQA()` 호출 시 파라미터 전달 |

**QAGenerationPanel 동작 흐름**
```
컴포넌트 마운트
  └─ GET /api/ingestion/hierarchy-list
       └─ doc_chunks metadata 스캔 → { l1_list, l2_by_l1 } 반환
            └─ L1 드롭다운 표시

L1 선택 → L2 드롭다운 표시 (해당 L1의 섹션 목록)
L2 선택 (선택사항, 생략 시 L1 전체 대상)

[생성 시작] 클릭
  └─ POST /api/generate { model, lang, samples, hierarchy_l1, hierarchy_l2 }
       └─ generation_api.py → job config에서 h1/h2 읽어 doc_chunks 필터링
```

### 4. Standardization → QA 생성 연결 버튼

태깅 완료 후 자연스럽게 다음 단계로 이동할 수 있도록 버튼 추가.

**변경 파일**

| 파일 | 변경 내용 |
|------|-----------|
| `frontend/src/components/standardization/DataStandardizationPanel.tsx` | `setActiveTab` prop 추가, 태깅 완료 시 "QA 생성으로 이동 →" 버튼 표시 |
| `frontend/src/App.tsx` | `<DataStandardizationPanel setActiveTab={setActiveTab} />` prop 전달 |

**UX 흐름**
```
Standardization 탭
  → 파일 업로드 → AI 분석 실행 → 태깅 샘플 표시 완료
       → "QA 생성으로 이동 →" 버튼 활성화 (isTagging=false && taggingSamples.length > 0)
            → 클릭 시 generation 탭으로 이동
                 → QAGenerationPanel 마운트 → DB에서 L1/L2 목록 자동 로드
```

---

## ✅ 추가 완료 작업 (2026-03-17 — P1 수정 + DB 스키마 재구성)

### 9. P1: `pipeline.py` — lang 하드코딩 수정 + generation_model 추가

`evaluation_results` 저장 시 `lang`이 항상 `"ko"`로 하드코딩되던 문제 수정.

```python
gen_config = result_data.get("config", {})
gen_lang   = gen_config.get("lang", "ko")
gen_model  = gen_config.get("model", "")

metadata={
    "generation_model": gen_model,   # 추가
    "evaluator_model":  evaluator_model,
    "lang":             gen_lang,    # 하드코딩 → 실제 값
    "prompt_version":   gen_config.get("prompt_version", "v1"),
}
```

---

### 10. P1-1: `generation_api.py` — heading/colophon 청크 skip

```python
_COLOPHON_KEYWORDS = ["발행처:", "발행인:", "저작권", "©", "무단전재", "재배포를 금"]

def _should_skip(chunk: dict) -> bool:
    if chunk.get("metadata", {}).get("chunk_type") == "heading":
        return True
    if sum(1 for kw in _COLOPHON_KEYWORDS if kw in chunk.get("content", "")) >= 2:
        return True
    return False
```

QA 생성 대상 청크 루프에서 skip 처리 + 로그 기록.

---

### 11. P1: `generation_api.py` — JSON fallback WARNING 로그 추가

```python
if not items:
    if is_supabase_available():
        logger.warning("⚠️ [FALLBACK] DB 청크 없음 또는 hierarchy 필터 미지정 → KT JSON 데이터로 대체...")
```

---

### 12. `supabase_client.py` — content_hash 중복 체크 추가

```python
# save_doc_chunk() 내부
content_hash = (metadata or {}).get("content_hash")
if content_hash:
    existing = supabase_client.table("doc_chunks").select("id") \
        .eq("metadata->>content_hash", content_hash).limit(1).execute()
    if existing.data:
        logger.debug(f"⏭️ Skipped duplicate chunk (hash={content_hash[:8]})")
        return None
```

재업로드 시 동일 청크 중복 INSERT 방지.

---

### 13. DB 스키마 재구성 — 테이블명 변경 + 뷰 추가

#### 테이블명 변경

| 구 이름 | 신규 이름 | 이유 |
|---------|----------|------|
| `evaluation_results` | `qa_eval_results` | `qa_` 접두어로 네이밍 통일 |
| `qa_generation_results` | `qa_gen_results` | 간결하게 축약 |

#### Supabase에 생성된 테이블 / 뷰

| 객체 | 유형 | 설명 |
|------|------|------|
| `qa_eval_results` | 테이블 | 4레이어 평가 파이프라인 결과 |
| `qa_gen_results` | 테이블 | QA 생성 결과 (qa_list 중첩 구조) |
| `qa_pairs_view` | 뷰 | qa_list 중첩을 flat하게 펼친 QA 샘플 확인용 |
| `evaluation_qa_joined` | 뷰 | qa_eval_results ↔ qa_gen_results 조인 뷰 |

#### 코드 변경 파일

| 파일 | 변경 내용 |
|------|-----------|
| `backend/config/supabase_client.py` | `.table("evaluation_results")` → `"qa_eval_results"` (2곳), `.table("qa_generation_results")` → `"qa_gen_results"` (3곳) |
| `backend/scripts/setup_qa_eval_tables.sql` | 테이블명 전체 치환 + `qa_pairs_view` 추가 |

#### `qa_pairs_view` 구조

```sql
CREATE OR REPLACE VIEW qa_pairs_view AS
SELECT
  qg.job_id, qg.created_at,
  doc->>'docId'                AS doc_id,
  doc->'hierarchy'             AS hierarchy,
  qa->>'q'                     AS question,
  qa->>'a'                     AS answer,
  qa->>'intent'                AS intent,
  (qa->>'answerable')::boolean AS answerable
FROM qa_gen_results qg,
  jsonb_array_elements(qg.qa_list) AS doc,
  jsonb_array_elements(doc->'qa_list') AS qa;
```

데이터 중복 없이 Supabase에서 QA 쌍 단위로 직접 확인 가능. 별도 RLS 불필요 (qa_gen_results RLS 상속).

---

## ✅ 파이프라인 전체 검증 완료 — 1차 (2026-03-17 11:09~11:11)

### 테스트 결과 (P0~P1 검증)

| 항목 | 결과 |
|------|------|
| `doc_chunks` 중복 방지 | ✅ 12개 (재업로드 후에도 동일, content_hash 중복 skip 동작) |
| `qa_gen_results` 저장 | ✅ `d4d48d12-ae02-43c9-93dc-51874f7b1d80` |
| `qa_eval_results` 저장 | ✅ `b126ce20-dba5-42a0-94b1-213a91d03cde` |
| `linked_evaluation_id` UPDATE | ✅ gen → eval 링크 성공 (RLS UPDATE 정책 동작) |
| Vector DB 경로 | ✅ "Using Vector DB retrieval" (JSON fallback 아님) |
| heading/colophon skip | ✅ 2 chunks 조회, 0 skipped |
| Layer 1-A Syntax | ✅ 16/16 pass (100%) |
| Layer 1-B Stats | ✅ integrated_score = 7.44 |
| Layer 2 RAG Triad | ✅ 16 QA evaluated |
| Layer 3 Quality | ✅ 13/16 pass (81.25%) |

**결론: P0 ~ P1 수정 사항 모두 정상 동작 확인. DB 기반 전체 파이프라인 최초 검증 완료.**

---

## ✅ 파이프라인 전체 검증 — 2차 (2026-03-17 13:36~13:40)

### P2 재인제스션 + P3 적응형 프롬프트 통합 테스트

#### doc_chunks 인제스션 결과

| 항목 | 결과 |
|------|------|
| 총 청크 수 | **11개** |
| TOC 청크 필터 | ✅ p1, p3 목차 3개 제거 |
| Colophon 필터 (p15) | ✅ 정상 필터링 (상세 아래) |
| chunk_type | list(8) / table(3) |

#### p15 페이지 필터링 이유

```
[13:36:13] INFO: Colophon chunk filtered (p15):
'AI 데이터 품질관리 가이드라인 v3.5 제2권 AI 데이터 구축 가이드 v3.5\n2025'
```

- **원인**: P2에서 구현한 `_is_colophon_chunk` 인제스션 레벨 필터 동작
- **P3 변경과 무관** — colophon 키워드 매칭으로 발행 정보 페이지 자동 제거
- **올바른 동작** — 버전·연도 정보만 있는 페이지는 QA 생성에 무의미

#### domain_profiler 동작 확인 (P3 신규)

```
Domain:   'AI 데이터 구축 및 품질 관리 가이드라인'
Audience: 데이터 구축 기관 및 프로젝트 관리자
Terms:    ['LLM (거대 언어 모델)', 'LMM (거대 멀티모달 모델)', '합성 데이터 (Synthetic Data)']
```

- doc_chunks 샘플 조회 → gpt-5.1 분석 → domain_profile 정상 생성
- 생성된 domain_profile이 QA 생성 프롬프트에 반영됨

#### QA 생성 및 평가 결과

| 항목 | 결과 | 1차 대비 |
|------|------|----------|
| 생성 모델 | gemini-3.1-flash | gpt-5.2 → gemini |
| `qa_gen_results` 저장 | ✅ `0de40bf8-9459-4a99-b59e-72b943e8f61b` | - |
| `qa_eval_results` 저장 | ✅ `ff0a1da5-27f7-458d-b4a8-920047ef4014` | - |
| `linked_evaluation_id` UPDATE | ✅ | - |
| Layer 1-A Syntax | ✅ 16/16 (100%) | 동일 |
| Layer 1-B Stats | ✅ integrated_score = **7.51** | 7.44 → +0.07 |
| Layer 2 RAG Triad | ✅ 16 QA evaluated | 동일 |
| Layer 3 Quality | ✅ **14/16 (87.5%)** | 81.25% → **+6.25%** |

**결론: P3 적응형 프롬프트 E2E 검증 완료. domain_profiler 정상 동작, Quality pass rate 향상 확인.**

---

## 📌 다음 To-Do

### ✅ 완료 항목

| 항목 | 우선순위 |
|------|---------|
| `config/prompts.py` 시스템 프롬프트 도메인 범용화 | P0-3 |
| `generation_api.py` dummy zero vector → `get_doc_chunks_by_filter()` | P0-2 |
| `generation_api.py` heading/colophon 청크 skip | P1-1 |
| `generation_api.py` JSON fallback WARNING 로그 | P1-4 |
| `pipeline.py` lang 하드코딩 수정 + generation_model 추가 | P1 |
| `supabase_client.py` content_hash 중복 체크 | P1 |
| `ingestion_api.py` GET /api/ingestion/hierarchy-list 엔드포인트 | P1-2 |
| `QAGenerationPanel.tsx` DB 기반 L1/L2 드롭다운 | P1-3 |
| DB 스키마 재구성 (`qa_eval_results`, `qa_gen_results`) | - |
| `qa_pairs_view`, `evaluation_qa_joined` 뷰 생성 | - |

### 🔲 남은 작업

#### P2 — Ingestion 품질 개선 ✅ 완료
- [x] `ingestion_api.py` — `normalize_text`에 `Ÿ` → `-` 정규화 + `_smart_join_lines` 추가
- [x] `ingestion_api.py` — 스플리터 후 `_merge_short_chunks` 후처리 (< 200자 청크 병합)
- [x] `ingestion_api.py` — `_is_colophon_chunk` 인제스션 레벨 필터 추가
- [x] doc_chunks 비우고 재인제스션 후 청크 품질 재검토 → **검증 완료**

#### P3 — 적응형 프롬프트 ✅ 완료
- [x] `backend/domain_profiler.py` 신규 — `analyze_domain()` 구현
- [x] `config/prompts.py` 빌더 함수화 — `build_system_prompt(domain_profile)`, `build_user_template(domain_profile, chunk_type)`
- [x] `generation_api.py` 2단계 흐름 통합 (job 시작 시 domain 분석 1회)
- [x] `main.py` — `generate_qa()` optional `system_prompt`, `user_template` 파라미터 추가

#### P4 — 선택적 개선
- [ ] `m.get("pages")` → `m.get("page")`, `m.get("type")` → `m.get("chunk_type")` 키 불일치 수정

#### Refactor — 백엔드 구조 개선 (별도 일정)
- [ ] 상세 계획 아래 참조

---

## 🏗️ 백엔드 구조 리팩토링 계획 (Backlog)

### 목표
- `backend/` 루트의 API 파일들 → `api/` 하위로 정리 (향후 report/dashboard 엔드포인트 확장 대비)
- `main.py ↔ generation_api.py` 순환 참조 해소
- `evaluators/` ↔ `generators/` 대칭 구조로 역할 경계 명확화

### 현재 문제

```
main.py  ──import──►  generation_api.py
   ▲                        │
   └─────lazy import─────── ┘  ← generate_qa를 main.py에서 가져옴 (순환)
```

`generate_qa()`가 `main.py`에 있어서 `generation_api.py`가 `main.py`를 역참조.
현재는 함수 내부 lazy import(`from main import generate_qa`)로 회피 중.

### 목표 구조

```
backend/
├── main.py                        # FastAPI 앱 초기화 + 라우터 등록만
├── api/
│   ├── __init__.py
│   ├── generation_api.py          # from generators.qa_generator import generate_qa
│   ├── evaluation_api.py
│   └── ingestion_api.py
│   └── (report_api.py 등 향후 추가)
├── generators/                    ← evaluators/ 대칭 구조
│   ├── __init__.py
│   ├── qa_generator.py            # get_client(), generate_qa_*(), generate_qa() 이동
│   └── domain_profiler.py         # Step 1: 도메인 분석 (현재 backend/ 루트에서 이동)
├── evaluators/                    # 기존 유지
│   ├── pipeline.py
│   ├── syntax_validator.py
│   ├── dataset_stats.py
│   ├── rag_triad.py
│   └── qa_quality.py
└── config/                        # 기존 유지
    ├── supabase_client.py
    ├── prompts.py
    ├── models.py
    └── constants.py
```

### 수정 필요 파일

| 파일 | 유형 | 변경 내용 |
|------|------|-----------|
| `main.py` | 수정 | `generate_qa` 관련 코드 → `generators/qa_generator.py`로 이동<br>import 경로: `from api.*.* import ...` |
| `generators/qa_generator.py` | **신규** | `get_client()`, `generate_qa_anthropic/google/openai()`, `generate_qa()` 이동 |
| `generators/domain_profiler.py` | 이동 | `backend/domain_profiler.py` → `backend/generators/domain_profiler.py` |
| `generators/__init__.py` | **신규** | 빈 파일 |
| `api/generation_api.py` | 이동+수정 | lazy import 제거 → `from generators.qa_generator import generate_qa`<br>`from generators.domain_profiler import analyze_domain` |
| `api/evaluation_api.py` | 이동 | sys.path 조정 |
| `api/ingestion_api.py` | 이동 | sys.path 조정 |
| `api/__init__.py` | **신규** | 빈 파일 |

### 프론트엔드 영향
- **없음** — HTTP 엔드포인트 경로(`/api/generate` 등) 변경 없음

### 진행 조건
- 현재 파이프라인 E2E 검증 완료 후 별도 작업으로 진행
- 리팩토링 전후 파이프라인 E2E 테스트 필수 (doc_chunks → QA 생성 → 평가 전체)

---

## ✅ P3 적응형 프롬프트 구현 완료 (2026-03-17)

### 구현된 흐름

```
POST /api/generate (job 시작)
    │
    ├─ [1단계] analyze_domain()                 ← domain_profiler.py (신규)
    │       doc_chunks에서 현재 hierarchy 범위 내 최대 10개 샘플 조회
    │       (content 앞 400자 + metadata: l1/l2/chunk_type)
    │       LLM 호출 (기본: gpt-5.1, temperature=0.2, max_tokens=2048)
    │       → domain_profile JSON 파싱
    │       실패 시 GENERIC_DOMAIN_PROFILE 자동 fallback
    │
    └─ [2단계] 청크별 QA 생성 루프              ← generation_api.py
            for chunk in filtered_chunks:
                chunk_type = chunk.metadata.chunk_type
                system_prompt = build_system_prompt(domain_profile, lang)
                user_template = build_user_template(domain_profile, chunk_type)
                generate_qa(item, model, lang, prompt_version,
                            system_prompt=system_prompt,
                            user_template=user_template)
```

### 구현 파일

| 파일 | 유형 | 주요 변경 |
|------|------|-----------|
| `backend/domain_profiler.py` | **신규** | `analyze_domain()` — 샘플링 → LLM 분석 → JSON 파싱 → fallback<br>`_call_llm()` — Claude/Gemini/OpenAI 분기 (temperature=0.2)<br>`GENERIC_DOMAIN_PROFILE` — fallback 기본값 |
| `backend/config/prompts.py` | **변경** | `build_system_prompt(domain_profile, lang)` 추가<br>`build_user_template(domain_profile, chunk_type, n_qa)` 추가<br>`_generate_intent_examples(domain_profile)` 추가<br>기존 상수(`SYSTEM_PROMPT_KO/EN_V1` 등) fallback으로 유지 |
| `backend/main.py` | **변경** | `generate_qa()` — `system_prompt`, `user_template` optional 파라미터 추가<br>주입값 우선 사용, 없으면 기존 상수 fallback |
| `backend/generation_api.py` | **변경** | 청크 루프 전 `analyze_domain()` 1회 호출<br>`_generate_one()` — chunk_type별 `build_system_prompt/user_template` 적용 |

### domain_profile JSON 구조

```json
{
  "domain": "AI 데이터 구축 가이드라인",
  "domain_short": "데이터 구축 가이드",
  "target_audience": "데이터 구축 작업자 및 프로젝트 담당자",
  "main_topics": ["품질 기준", "프로세스 관리", "유형별 구축 사례"],
  "key_terms": ["청크", "레이블링", "품질 오류", "데이터 공정", "계층 구조"],
  "chunk_type_dist": {"table": 3, "list": 8},
  "intent_hints": {
    "table": ["numeric", "list", "boolean"],
    "list": ["procedure", "how", "list"],
    "body": ["factoid", "why", "definition"],
    "heading": "skip"
  },
  "tone": "기술 문서 (격식체, 객관적 설명 중심)"
}
```

### 주요 설계 결정

| 항목 | 결정 | 이유 |
|------|------|------|
| 도메인 분석 모델 기본값 | `gpt-5.1` | 안정적인 JSON 출력 |
| temperature | `0.2` | JSON 구조 안정성 확보 |
| 분석 프롬프트 언어 | 영문 (응답은 한국어 강제) | LLM 지시 영문이 더 정확, 출력 용어는 한국어 |
| fallback | `GENERIC_DOMAIN_PROFILE` | 분석 실패 시 생성 파이프라인 중단 없음 |
| 분석 횟수 | job당 1회 | 모든 청크에 동일 domain_profile 재사용 |

### chunk_type별 intent 가중치

| chunk_type | 권장 intent 유형 | 비고 |
|------------|-----------------|------|
| `table` | numeric, list, boolean | 수치·비교형 |
| `list` | procedure, how, list | 절차형 |
| `body` | factoid, why, definition | 설명형 |
| `heading` | — | **전량 skip** |

### fallback 전략

```python
GENERIC_DOMAIN_PROFILE = {
    "domain": "문서",
    "domain_short": "문서",
    "target_audience": "독자",
    "main_topics": [],
    "key_terms": [],
    "chunk_type_dist": {},
    "intent_hints": {
        "table": ["numeric", "list", "boolean"],
        "list": ["procedure", "how", "list"],
        "body": ["factoid", "why", "definition"],
        "heading": "skip"
    },
    "tone": "격식체"
}
```
- LLM 도메인 분석 실패 또는 JSON 파싱 오류 시 자동 적용
- 기존 범용 프롬프트와 동일한 품질 보장

### 구현 순서

1. `domain_profiler.py` 신규 작성
2. `prompts.py` 빌더 함수 추가 (기존 상수 유지)
3. `generation_api.py` 2단계 흐름 통합
4. 기존 테스트 데이터로 domain_profile 생성 결과 확인

#### P4 — 선택적 개선
- [ ] `generation_api.py` — `chunk_type`별 intent 가중치 분기 (table→numeric, list→procedure)
- [ ] `m.get("pages")` → `m.get("page")`, `m.get("type")` → `m.get("chunk_type")` 키 불일치 수정

---

## 🔍 QA 생성 파이프라인 전체 검토 (2026-03-17 추가)

### 현재 데이터 흐름 (As-Is)

```
[경로 1: JSON Fallback] — 현재 실제 동작 경로
  ref/data/data_2026-03-06_normalized.json (1,106개 KT 웹 스크래핑 문서)
    └─ { docId, hierarchy: ["Shop","USIM","선불USIM"], title, text(웹본문), url }
         └─ generate_qa(item)
              ├─ text[:2000] → 프롬프트에 삽입
              └─ hierarchy 문자열 조합 → [카테고리] 라벨

[경로 2: Vector DB] — hierarchy 필터가 있을 때만 진입
  Supabase doc_chunks
    └─ search_doc_chunks(dummy_zero_vector, filter={l1/l2/l3})
         └─ { id, content, metadata.hierarchy_l1/l2/l3 }
              └─ item = { docId: id, hierarchy: [l1,l2,l3], text: content }
```

### 문제점 분류

#### 🔴 크리티컬 (QA 생성 품질 치명적)

**1. 프롬프트 도메인 하드코딩**
```python
# config/prompts.py 현재
SYSTEM_PROMPT_KO_V1 = """당신은 통신사 고객지원 QA 데이터셋 생성 전문가입니다."""
```
→ 현재 문서(AI 데이터 구축 가이드)와 전혀 맞지 않음. **범용 문서 도메인**으로 교체 필요.

**2. DB 청크 text 필드에 context prefix 포함**
```python
# backend/main.py line 278
text = item.get("text", "")[:2000]
```
DB 청크의 `content`에는 `[문서] ... [섹션] ... [페이지] ...\n\n[내용]\n...` 형식의 prefix가 포함됨.
`text[:2000]`으로 그대로 프롬프트에 넣으면 LLM이 구조 메타 텍스트를 본문으로 오인.
→ `raw_text` (순수 본문)를 별도 저장하거나, prefix를 제거하는 전처리 필요.

**3. dummy zero vector — semantic 검색 무력화**
```python
# generation_api.py line 303
if query_vector is None:
    query_vector = [0.0] * 1536  # Dummy for metadata filter only
```
→ `match_doc_chunks` RPC가 내부적으로 cosine similarity를 계산하므로, zero vector는 결과가 불확정적.  
→ Supabase `.table("doc_chunks").select().eq("metadata->>hierarchy_l1", h1)` 방식의 직접 필터로 교체하거나,  
  L1/L2 레이블 텍스트 자체를 embedding해서 semantic query로 활용.

#### 🟡 중요 (DB 기반 워크플로우 미완성)

**4. 프론트엔드 hierarchy 하드코딩**
```tsx
// QAGenerationPanel.tsx — 현재
const hierarchy = ["Shop", "USIM/eSIM 가입", "선불 USIM 구매/충전"];
```
→ DB에 있는 실제 L1/L2 목록을 동적으로 가져오는 API 및 선택 UI 없음.  
→ `generateQA()` 호출 시 `hierarchy_l1/l2/l3` 파라미터도 미전달 (항상 JSON fallback 경로).

**5. JSON fallback 의존성**
→ `ref/data/data_2026-03-06_normalized.json`은 KT 통신사 데이터. 범용 시스템으로 전환되면 무의미.  
→ fallback을 명시적 오류 또는 deprecation 경고로 교체해야 DB 경로만 동작하게 됨.

#### 🟢 개선 (선택적)

**6. chunk_type별 QA 전략 미분화**
- `table` 청크 → 수치 비교형 QA (`numeric`, `list` 의도)
- `list` 청크 → 절차형 QA (`procedure`, `how` 의도)
- `body` 청크 → 설명형 QA (`factoid`, `why`, `definition` 의도)
- `heading` 청크 단독 → QA 생성 의미 없음 (skip)

현재는 모든 청크에 동일한 8-intent 프롬프트 적용.

---

### JSON(기존) vs DB(신규) 스키마 비교

| 항목 | JSON 기존 | DB chunk 신규 |
|------|-----------|----------------|
| 텍스트 필드 | `item["text"]` (웹 원문 수KB) | `content` (청킹 200~1200자) |
| context prefix | 없음 | `[문서]...[섹션]...[페이지]...` 포함 |
| hierarchy | `["Shop","USIM","선불USIM"]` (URL 기반) | `hierarchy_l1/l2/l3` (AI 태깅) |
| 문서 식별자 | `docId: "ktcom_3842"` | `id: UUID` |
| 추가 메타 | `url`, `title`, `startdate`, `status` | `chunk_type`, `section_path`, `keywords`, `page` |

---

### 수정 범위 요약

```
크리티컬 (즉시 수정)
  ├─ config/prompts.py       시스템 프롬프트 범용화
  ├─ backend/main.py         text 필드에서 context prefix 제거
  ├─ generation_api.py       dummy zero vector → 직접 select 필터 방식으로 교체
  └─ generation_api.py       heading/발행처 청크 생성 skip

중요 (DB 워크플로우 완성)
  ├─ backend (신규 API)      GET /api/ingestion/hierarchy-list → DB L1/L2 목록 반환
  ├─ QAGenerationPanel.tsx   hierarchy 동적 드롭다운 + generateQA() 파라미터 전달
  └─ config/constants.py     JSON fallback 제거/경고

선택적 개선
  ├─ prompts.py              chunk_type별 system prompt 분기
  └─ generation_api.py       chunk_type별 QA 의도 유형 가중치
```

---

## 🔍 참고: 현재 DB 상태 (2026-03-17 기준)

### P2 재인제스션 후 (최신)

```
총 11개 청크 (테스트 문서: 테스트데이터_1.pdf)

chunk_type 분포: list(8) / table(3)
L1 분포: 가이드라인 개요(5) / 데이터 구축 공정(3) / 품질 및 오류 분석(2) / 유형별 구축 사례(1)

글자 수: avg=773 / min=242 / max=1,177
```

| 변경 항목 | 기존 (12개) | 재인제스션 후 (11개) |
|-----------|------------|----------------------|
| heading 청크 | 1개 (57자, 단독 제목) | 0개 — 인접 청크에 병합 |
| body 청크 (발행처) | 1개 | 0개 — colophon 필터 제거 |
| 200자 미만 청크 | [3]=57자, [7]=258자 | 0개 — merge_short_chunks 동작 |
| chunk_type | table/list/heading/body | table/list 만 존재 |

### P2 이전 (참고용)

```
총 12개 청크

[3]  page=9   chunk_type=heading  h1=가이드 개요       h2=정보 체계 정의    ← 57자, 단독 제목
[7]  page=12  chunk_type=list     h1=유형별 구축 사례  h2=데이터 전략 수립  ← 258자, 짧음
[11] page=15  chunk_type=body     h1=가이드 개요       h2=문서 소유권 및 이력  ← 발행처 페이지
```

---

## ✅ 백엔드 디렉토리 리팩토링 완료

### 변경 구조

```
backend/
├── api/                        ← 신규 (API 라우트 전담)
│   ├── __init__.py
│   ├── generation_api.py       ← generation_api.py 이동
│   ├── evaluation_api.py       ← evaluation_api.py 이동
│   └── ingestion_api.py        ← ingestion_api.py 이동
├── generators/                 ← 신규 (생성 로직 전담)
│   ├── __init__.py
│   ├── qa_generator.py         ← main.py의 generate_qa 관련 코드 분리
│   └── domain_profiler.py      ← domain_profiler.py 이동
├── evaluators/                 ← 기존 유지
├── config/                     ← 기존 유지
└── main.py                     ← API 통합 허브 (generate_qa 코드 제거)
```

### 주요 변경 사항

| 파일 | 변경 내용 |
|------|-----------|
| `main.py` | `generate_qa` 관련 함수 4개 제거 → `generators/qa_generator.py` 이동 |
| `main.py` | import 경로 `api.*` 로 업데이트 (`from api.generation_api import ...` 등) |
| `main.py` | 미사용 import 정리 (`ThreadPoolExecutor`, `MODEL_CONFIG`, prompts 상수 등) |
| `api/generation_api.py` | `sys.path` 수정 (`parent` → `parent.parent`), `generators.*` import 적용 |
| `generators/qa_generator.py` | `get_client()`, `generate_qa_anthropic/google/openai()`, `generate_qa()` 포함 |
| `generators/domain_profiler.py` | `analyze_domain()`, `GENERIC_DOMAIN_PROFILE` 포함 |

### Import 변경 내역

```python
# main.py (before → after)
from generation_api import ...    → from api.generation_api import ...
from evaluation_api import ...    → from api.evaluation_api import ...
from ingestion_api import ...     → from api.ingestion_api import ...

# api/generation_api.py (before → after)
sys.path.insert(0, str(Path(__file__).parent))          # api/ 추가 (잘못됨)
→ sys.path.insert(0, str(Path(__file__).parent.parent)) # backend/ 추가 (수정)

from main import generate_qa               → from generators.qa_generator import generate_qa
from domain_profiler import analyze_domain → from generators.domain_profiler import analyze_domain
```

### 검증 결과

```
python -c "
from generators.qa_generator import generate_qa, get_client
from generators.domain_profiler import analyze_domain, GENERIC_DOMAIN_PROFILE
from api.generation_api import JobManager, setup_generation_routes
from api.evaluation_api import setup_evaluation_routes
from api.ingestion_api import setup_ingestion_routes
"
→ All imports OK (Supabase 미연결 경고 1건 — 정상)
```

---

## 🔍 문서 스코프 세션 플로우 설계

### DB 구조 확인

`doc_chunks` 테이블에 별도 `doc_name` 컬럼 없음. 문서 식별자는 `metadata` JSONB 내 `filename` key로 저장:

```json
doc_chunks.metadata = {
  "filename": "테스트데이터_2.pdf",
  "hierarchy_l1": "가이드 개요",
  "hierarchy_l2": "문서 이력 관리",
  "chunk_type": "body",
  "page": 3,
  "content_hash": "..."
}
```

### 현재 문제 — 전역 혼합

| 함수 | 현재 동작 | 문제 |
|------|-----------|------|
| `get_hierarchy_list()` | 전체 doc_chunks 조회 | 문서A + 문서B hierarchy 혼합 반환 |
| `get_doc_chunks_by_filter()` | L1/L2/L3 필터만 | 동일 L1/L2를 가진 다른 문서 청크 포함 |
| `analyze_domain()` | L1/L2 필터만 | 다른 문서 샘플로 도메인 분석 오염 가능 |

→ 문서B 업로드·hierarchy 완료 후 Generation 이동해도, 드롭다운에 문서A의 L1/L2 노출됨
→ 잘못된 L1/L2 선택 시 다른 문서 청크에서 QA 생성될 위험

### 세션 ID 체계 — 별도 세션 ID 불필요

이미 있는 ID 3개를 프론트엔드 state로 연결하면 충분:

```
[업로드]
  → filename (metadata.filename)         ← 문서 스코프 키 (문자열)
       ↓
[Hierarchy 태깅]
  → filename 기준 태깅 (ingestion_api — 이미 구현됨)
       ↓
[QA 생성]
  → filename 필터로 hierarchy 조회
  → filename 필터로 청크 조회
  → generation_job_id (UUID)             ← 생성 추적 키
       ↓
[평가]
  → eval_job_id (UUID)                   ← 평가 추적 키
  → linked_generation_id                 ← 생성 ↔ 평가 연결
```

**App.tsx에서 관리할 state:**

```typescript
const [currentFilename, setCurrentFilename] = useState<string | null>(null); // 업로드 시 set
const [lastGenJobId, setLastGenJobId]       = useState<string | null>(null); // 생성 완료 시 set
const [lastEvalJobId, setLastEvalJobId]     = useState<string | null>(null); // 평가 완료 시 set
```

### 수정 계획

#### Backend (3곳)

**1. `config/supabase_client.py` — `get_hierarchy_list(filename=None)`**

```python
async def get_hierarchy_list(filename: str = None) -> Dict[str, Any]:
    query = supabase_client.table("doc_chunks").select("metadata")
    if filename:
        query = query.eq("metadata->>filename", filename)   # 기존 패턴과 동일
    response = query.execute()
    ...
```

**2. `config/supabase_client.py` — `get_doc_chunks_by_filter(filename=None)`**

```python
async def get_doc_chunks_by_filter(
    hierarchy_l1=None, hierarchy_l2=None, hierarchy_l3=None,
    filename: str = None,
    limit: int = 20,
) -> list:
    query = supabase_client.table("doc_chunks").select("id, content, metadata")
    if filename:
        query = query.eq("metadata->>filename", filename)
    if hierarchy_l1:
        query = query.eq("metadata->>hierarchy_l1", hierarchy_l1)
    ...
```

**3. `api/ingestion_api.py` — query param 수용**

```python
@router.get("/hierarchy-list")
async def get_hierarchy_list_endpoint(filename: str = None):
    return await get_hierarchy_list(filename=filename)
```

#### Frontend (4곳)

**4. `frontend/src/lib/api.ts` — `getHierarchyList(filename?)`**

```typescript
export async function getHierarchyList(filename?: string) {
  const params = filename ? `?filename=${encodeURIComponent(filename)}` : "";
  return fetch(`${API_BASE}/api/ingestion/hierarchy-list${params}`).then(r => r.json());
}
```

**5. `frontend/src/App.tsx` — state 3개 + 콜백 + 탭 자동 전환**

```typescript
const [currentFilename, setCurrentFilename] = useState<string | null>(null);
const [lastGenJobId, setLastGenJobId]       = useState<string | null>(null);
const [lastEvalJobId, setLastEvalJobId]     = useState<string | null>(null);

// StandardizationPanel → 업로드 완료 시
<StandardizationPanel onUploadComplete={(filename) => setCurrentFilename(filename)} />

// QAGenerationPanel → filename 전달 + 완료 콜백
<QAGenerationPanel
  currentFilename={currentFilename}
  onGenComplete={(jobId) => setLastGenJobId(jobId)}
  onEvalComplete={(jobId) => { setLastEvalJobId(jobId); setActiveTab("evaluation"); }}
/>

// QAEvaluationDashboard → evalJobId 전달
<QAEvaluationDashboard evalJobId={lastEvalJobId} />
```

**6. `frontend/src/components/generation/QAGenerationPanel.tsx`**

- `currentFilename` prop 추가
- hierarchy 로드 시: `getHierarchyList(currentFilename)` 호출
- generation 요청 payload에 `filename: currentFilename` 포함
- `onGenComplete` / `onEvalComplete` 콜백 prop 추가

**7. `frontend/src/components/standardization/` (업로드 패널)**

- 업로드 성공 응답에서 `filename` 추출 → `onUploadComplete(filename)` 콜백 호출

#### Generation API — filename 청크 필터 전파

`api/generation_api.py:run_qa_generation_real()`:

```python
filename = config.get("filename")   # 신규 추가

chunks = await get_doc_chunks_by_filter(
    hierarchy_l1=h1,
    hierarchy_l2=h2,
    hierarchy_l3=h3,
    filename=filename,              # 신규 추가
    limit=samples,
)
```

`generators/domain_profiler.py:analyze_domain()`:

```python
samples = await get_doc_chunks_by_filter(
    hierarchy_l1=hierarchy_l1,
    hierarchy_l2=hierarchy_l2,
    filename=filename,              # 신규 추가
    limit=10,
)
```

### 영향 파일 요약

| 파일 | 변경 내용 |
|------|-----------|
| `backend/config/supabase_client.py` | `get_hierarchy_list(filename)`, `get_doc_chunks_by_filter(filename)` |
| `backend/api/ingestion_api.py` | `GET /hierarchy-list?filename=` 쿼리 파라미터 |
| `backend/api/generation_api.py` | config에서 `filename` 읽어 필터 전달 |
| `backend/generators/domain_profiler.py` | `analyze_domain(filename)` 파라미터 추가 |
| `frontend/src/lib/api.ts` | `getHierarchyList(filename?)` |
| `frontend/src/App.tsx` | state 3개 + 콜백 + 탭 전환 |
| `frontend/src/components/generation/QAGenerationPanel.tsx` | `currentFilename` prop + 콜백 |
| `frontend/src/components/standardization/` | `onUploadComplete(filename)` 콜백 |

---

## 📋 Evaluation 페이지 연결 계획

### 현재 상태

- `QAEvaluationDashboard.tsx` — 전부 하드코딩된 mock 데이터 (KT 통신사 샘플), API 연결 없음
- `QAGenerationPanel.tsx` — 생성 완료 시 `POST /api/evaluate` 자동 호출 + 폴링 이미 구현됨
  - `evalLayers` (4레이어 상태), `evalReport` (완료 데이터) 내부에만 존재
  - Dashboard로 전달되는 경로 없음

### Phase 1 — App.tsx 브릿지 (탭 자동 전환)

위 세션 플로우의 `onEvalComplete` → `setLastEvalJobId` → `setActiveTab("evaluation")`
→ 생성 완료 시 자동으로 Evaluation 탭으로 이동

### Phase 2 — Dashboard 실데이터 연결

**목표**: mock 제거 → `GET /api/evaluate/{eval_job_id}/status` 응답으로 차트 렌더링

**API 응답 구조 확인 필요** (`evaluation_api.py` 실제 반환 필드 점검):

```json
{
  "status": "completed",
  "layers": {
    "syntax":  { "score": 0.92, "details": {...} },
    "stats":   { "score": 0.87, "details": {...} },
    "rag":     { "score": 0.81, "details": {...} },
    "quality": { "score": 0.78, "details": {...} }
  },
  "eval_report": {
    "final_score": 0.84,
    "grade": "A",
    "qa_list": [{ "question": "...", "answer": "...", "intent": "...", ... }]
  }
}
```

**Dashboard 차트 매핑**:

| 차트 | mock → 실데이터 |
|------|----------------|
| Layer Score 카드 4개 | `layers.{syntax,stats,rag,quality}.score` |
| Intent Distribution (Pie) | `eval_report.qa_list[].intent` 집계 |
| Dataset Stats (Radar) | `layers.stats.details` |
| Quality Scores (Bar) | `layers.rag.details` + `layers.quality.details` |
| QA Table | `eval_report.qa_list[]` |

### Phase 3 — 이력 조회 (과거 평가 결과 브라우징)

**Backend 추가**: `GET /api/evaluate/list`

```python
@router.get("/list")
async def list_evaluations():
    response = (supabase_client.table("qa_eval_results")
        .select("id, created_at, final_score, grade, generation_id")
        .order("created_at", desc=True)
        .execute())
    return response.data
```

**Frontend**: Dashboard 상단 드롭다운 → 선택 시 해당 eval_job_id로 차트 갱신

### 구현 순서

```
[세션 플로우] Backend 3곳 → Frontend 4곳
     ↓
[Eval Phase 1] App.tsx 브릿지 + 탭 전환  (세션 플로우와 동시 진행 가능)
     ↓
[Eval Phase 2] Dashboard mock 제거 → 실데이터 연결
     ↓
[Eval Phase 3] 이력 조회 엔드포인트 + UI
```

**영향 파일 요약**:
- `frontend/src/App.tsx`
- `frontend/src/components/generation/QAGenerationPanel.tsx`
- `frontend/src/components/evaluation/QAEvaluationDashboard.tsx`
- `frontend/src/lib/api.ts`
- `backend/api/evaluation_api.py` (Phase 3)

---

## ✅ 문서 스코프 세션 플로우 구현 완료

### Backend 수정 (3곳)

**`backend/config/supabase_client.py`**

```python
# get_doc_chunks_by_filter — filename 파라미터 추가
async def get_doc_chunks_by_filter(
    hierarchy_l1=None, hierarchy_l2=None, hierarchy_l3=None,
    filename: Optional[str] = None,   # ← 신규
    limit: int = 20,
) -> list:
    ...
    if filename:
        query = query.eq("metadata->>filename", filename)  # filename 먼저 적용
    ...

# get_hierarchy_list — filename 파라미터 추가
async def get_hierarchy_list(filename: Optional[str] = None) -> Dict[str, Any]:
    ...
    query = supabase_client.table("doc_chunks").select("metadata")
    if filename:
        query = query.eq("metadata->>filename", filename)
    response = query.execute()
    ...
```

**`backend/api/ingestion_api.py`**

```python
@router.get("/hierarchy-list")
async def get_hierarchy_list_endpoint(filename: str = None):
    # GET /api/ingestion/hierarchy-list?filename=테스트데이터_2.pdf
    result = await get_hierarchy_list(filename=filename)
    return {"success": True, **result}
```

### Frontend 수정 (5곳)

**`frontend/src/lib/api.ts`**

```typescript
// GenerateRequest — filename 필드 추가
interface GenerateRequest { ...; filename?: string; ... }

// getHierarchyList — filename 파라미터 추가
export async function getHierarchyList(filename?: string) {
  const params = filename ? `?filename=${encodeURIComponent(filename)}` : "";
  return fetch(`${API_BASE}/api/ingestion/hierarchy-list${params}`).then(r => r.json());
}
```

**`frontend/src/components/standardization/DataStandardizationPanel.tsx`**

```typescript
// onUploadComplete prop 추가
export function DataStandardizationPanel({ setActiveTab, onUploadComplete }: {
  setActiveTab?: (tab: string) => void;
  onUploadComplete?: (filename: string) => void;
})

// 업로드 성공 시 콜백 호출
setUploadedFilename(fileName);
onUploadComplete?.(fileName);  // ← 신규
```

**`frontend/src/components/generation/QAGenerationPanel.tsx`**

```typescript
// props 추가
interface QAGenerationPanelProps {
  currentFilename?: string | null;
  onEvalComplete?: (evalJobId: string) => void;
}

// hierarchy 로드: currentFilename 변경 시 재로드 + 필터 적용
useEffect(() => { loadHierarchyList(); }, [currentFilename]);
const result = await getHierarchyList(currentFilename ?? undefined);

// generation 요청에 filename 포함
...(currentFilename && { filename: currentFilename }),

// 평가 완료 시 콜백
onEvalComplete?.(evalJobId);
```

**`frontend/src/components/evaluation/QAEvaluationDashboard.tsx`**

```typescript
// evalJobId prop 추가 (Phase 2 실데이터 연결 준비)
export function QAEvaluationDashboard({ evalJobId }: { evalJobId?: string | null } = {})
```

**`frontend/src/App.tsx`**

```typescript
// state 2개 추가
const [currentFilename, setCurrentFilename] = useState<string | null>(null);
const [lastEvalJobId, setLastEvalJobId]     = useState<string | null>(null);

// 콜백 연결
<DataStandardizationPanel
  onUploadComplete={(filename) => setCurrentFilename(filename)}
/>
<QAGenerationPanel
  currentFilename={currentFilename}
  onEvalComplete={(evalJobId) => { setLastEvalJobId(evalJobId); setActiveTab("evaluation"); }}
/>
<QAEvaluationDashboard evalJobId={lastEvalJobId} />
```

### 완료된 세션 플로우

```
업로드 (DataStandardizationPanel)
  → onUploadComplete("테스트데이터_2.pdf")
  → App.currentFilename = "테스트데이터_2.pdf"
        ↓
Generation (QAGenerationPanel)
  → getHierarchyList("테스트데이터_2.pdf")  ← 해당 문서 L1/L2만 드롭다운에 표시
  → generateQA({ filename: "테스트데이터_2.pdf", hierarchy_l1: ..., ... })
  → 평가 완료 → onEvalComplete(evalJobId)
  → App.lastEvalJobId = "uuid..." + setActiveTab("evaluation")
        ↓
Evaluation (QAEvaluationDashboard)
  → evalJobId prop 수신 (Phase 2에서 실데이터 연결 예정)
```

### 미완료 (다음 작업)

| 항목 | 내용 |
|------|------|
| Generation API `filename` 수신 | `api/generation_api.py` config에서 `filename` 읽어 `get_doc_chunks_by_filter`에 전달 |
| `domain_profiler` `filename` 전파 | `analyze_domain()` → `get_doc_chunks_by_filter(filename=filename)` |
| Evaluation Phase 2 | `QAEvaluationDashboard` mock → 실 API 연결 |

**신규 작성**: `frontend/README.md` — 디렉토리 구조, 세션 플로우, Props 명세, API 클라이언트 정리

---

## 📐 Hierarchy 트리 프리뷰 계획

### 목표

Standardization 페이지에서 hierarchy 태깅 완료 후, 적용된 전체 L1/L2/L3 구조를 카드 트리 형태로 보여줌.

### 현재 한계

- `taggingSamples` — 샘플 일부(5~10개)만 포함, 전체 hierarchy 아님
- `analysis.l1_candidates` — L1 목록만, L2/L3 없음
- `get_hierarchy_list()` — L1/L2만 반환, L3 미포함

### 구현 계획

#### Backend — `get_hierarchy_list()` L3 추가 (5줄)

```python
# config/supabase_client.py
l2_by_l1: Dict[str, set] = {}
l3_by_l2: Dict[str, set] = {}   # ← 신규

for chunk in chunks:
    l1 = meta.get("hierarchy_l1")
    l2 = meta.get("hierarchy_l2")
    l3 = meta.get("hierarchy_l3")   # ← 신규
    if l1:
        l2_by_l1[l1].add(l2)
        if l2 and l3:
            l3_by_l2[f"{l1}::{l2}"].add(l3)   # ← 신규

return {
    "l1_list": sorted(l2_by_l1.keys()),
    "l2_by_l1": {k: sorted(v) for k, v in l2_by_l1.items()},
    "l3_by_l2": {k: sorted(v) for k, v in l3_by_l2.items()},   # ← 신규
}
```

API 응답 구조:
```json
{
  "l1_list": ["가이드 개요", "데이터 구축 공정"],
  "l2_by_l1": {
    "가이드 개요": ["문서 이력 관리", "데이터 품질 전략"]
  },
  "l3_by_l2": {
    "가이드 개요::문서 이력 관리": ["버전 관리", "발행 기준"],
    "가이드 개요::데이터 품질 전략": ["검증 절차", "품질 지표"]
  }
}
```

#### Frontend — 태깅 완료 후 트리 카드 렌더링

**트리뷰 UI 형태:**
```
┌──────────────────────────────────────┐
│ 📁 가이드 개요                  L1  │
│   ├── 📂 문서 이력 관리         L2  │
│   │     [버전 관리] [발행 기준] L3  │
│   └── 📂 데이터 품질 전략       L2  │
│         [검증 절차] [품질 지표] L3  │
├──────────────────────────────────────┤
│ 📁 데이터 구축 공정             L1  │
│   └── 📂 청킹 전략              L2  │
│         [구분 기준] [병합 조건] L3  │
└──────────────────────────────────────┘
```

**추가 로직:**
1. 태깅 완료 조건 (`!isTagging && !isAnalyzingSamples && taggingSamples.length > 0`) 충족 시
2. `getHierarchyList(uploadedFilename)` 호출 → 전체 L1/L2/L3 구조 수신
3. L1별 카드 그리드로 렌더링, L2는 트리 항목, L3는 배지

**컴포넌트 내 신규 state:**
```typescript
const [hierarchyTree, setHierarchyTree] = useState<{
  l1_list: string[];
  l2_by_l1: Record<string, string[]>;
  l3_by_l2: Record<string, string[]>;
} | null>(null);
```

**트리 로드 타이밍:**
```typescript
// runTagging() 완료 후 (apply-granular-tagging 성공 시)
const treeData = await getHierarchyList(filename);
setHierarchyTree(treeData);
```

#### 영향 파일

| 파일 | 변경 내용 |
|------|-----------|
| `backend/config/supabase_client.py` | `get_hierarchy_list()` — `l3_by_l2` 수집 추가 |
| `frontend/src/lib/api.ts` | `getHierarchyList()` 반환 타입에 `l3_by_l2` 추가 |
| `frontend/src/components/standardization/DataStandardizationPanel.tsx` | 태깅 완료 후 트리 카드 UI 추가 |

#### 복잡도

낮음. 기존 루프에 항목 추가 + 프론트 중첩 `map()` 렌더링. 신규 API 엔드포인트 불필요.

---

## ✅ 평가 파이프라인 — Supabase 직접 읽기로 전환

### 원인

```
generation_api.py  BASE_DIR = Path(__file__).parent.parent
                   = /autoeval/backend/api/../../  → /autoeval/backend
                   OUTPUT_DIR = /autoeval/backend/output  ← 여기 저장

pipeline.py        BASE_DIR = Path(__file__).parent.parent.parent
                   = /autoeval/backend/evaluators/../../../  → /autoeval
                   OUTPUT_DIR = /autoeval/output             ← 여기서 찾음
```

생성은 `/autoeval/backend/output/`에 저장, 평가는 `/autoeval/output/`에서 탐색 → `FileNotFoundError`

### 수정 방향

파일 경로 의존성 제거. `generation_id` 있으면 Supabase `qa_gen_results`에서 직접 `qa_list` 읽기.

`qa_gen_results` 테이블에 이미 `qa_list` 전체가 저장되어 있고, `generation_id`가 평가 파이프라인으로 전달되므로 파일 불필요.

### 수정 내용

**`backend/config/supabase_client.py` — `get_qa_generation_from_supabase()` 추가**

```python
async def get_qa_generation_from_supabase(generation_id: str) -> Optional[Dict[str, Any]]:
    response = (
        supabase_client.table("qa_gen_results")
        .select("metadata, qa_list")
        .eq("id", generation_id)
        .single()
        .execute()
    )
    return response.data if response.data else None
```

**`backend/evaluators/pipeline.py` — `run_evaluation()` 데이터 로드 로직 교체**

```python
# generation_id 있으면 Supabase에서 직접 읽기
if generation_id:
    gen_data = asyncio.run(get_qa_generation_from_supabase(generation_id))
    qa_list  = gen_data.get("qa_list", [])
    gen_lang = gen_data["metadata"].get("lang", "ko")
else:
    # fallback: 로컬 파일 (generation_id 없는 레거시 케이스)
    result_filepath = OUTPUT_DIR / result_filename
    if not result_filepath.exists():
        raise FileNotFoundError(...)
    ...
```

### 흐름

```
generation 완료
  → qa_list Supabase 저장 (generation_id 반환) ✓
  → 로컬 JSON 저장 (경로 무관, fallback용)

evaluation 시작 (generation_id 수신)
  → get_qa_generation_from_supabase(generation_id)
  → qa_gen_results.qa_list 직접 사용 ✓
  → FileNotFoundError 없음
```

---

## ✅ Generation 패널 — Hierarchy 빈값 처리 수정

### 문제

`currentFilename = null` 상태에서 Generation 패널 진입 시 `getHierarchyList(undefined)` 호출 → 전체 DB hierarchy 반환

### 수정

**`frontend/src/components/generation/QAGenerationPanel.tsx`**

```typescript
const loadHierarchyList = async () => {
  setSelectedL1(""); setSelectedL2("");
  if (!currentFilename) {
    // filename 없으면 API 호출하지 않고 빈값 세팅
    setHierarchyL1List([]);
    setHierarchyL2Map({});
    setHierarchyLoaded(false);
    return;
  }
  setIsLoadingHierarchy(true);
  const result = await getHierarchyList(currentFilename);
  ...
};
```

빈값 상태 안내 메시지도 컨텍스트에 따라 분기:

```typescript
{currentFilename
  ? "Hierarchy 정보가 없습니다. 태깅이 완료된 문서인지 확인해주세요."
  : "Standardization 탭에서 문서를 업로드하면 해당 문서의 계층이 표시됩니다."}
```

---

## ✅ Standardization 패널 — UI 텍스트 수정

**`frontend/src/components/standardization/DataStandardizationPanel.tsx`**

| 위치 | 기존 | 변경 |
|------|------|------|
| 카드 제목 | `계층 구조 분석 (Hierarchy)` | `컨텍스트 분석 (Hierarchy)` |
| 설명 | `AI가 문서를 분석하여 L1/L2/L3 계층 태깅을 자동으로 적용합니다.` | `LLM이 문서 도메인을 분석하고 L1/L2/L3 계층 구조를 자동으로 태깅합니다.` |
| 버튼 | `AI 분석 실행` | `컨텍스트 분석` |

---

## ✅ QA Generation 패널 — UI 전면 재설계 (2026-03-17)

### 레이아웃 변경

| 항목 | 기존 | 변경 |
|------|------|------|
| 상단 구조 | 단일 카드 전체 | `grid grid-cols-3` — Settings(col-span-2) + Hierarchy(col-span-1) 동일 높이 |
| 터미널 | 설정 카드 내부 일부 | 전체 너비 `bg-slate-950` 독립 블록 (h-52) |
| 평가 완료 카드 | 즉시 노출 | AnimatePresence + motion.div 슬라이드-업 애니메이션 |

### Layer 라벨 이모지 제거

```
기존: 1️⃣ 구문 검증  2️⃣ 데이터셋 통계  3️⃣ RAG Triad  4️⃣ 품질 평가
변경: Layer 1-A  구문 검증  /  Layer 1-B  데이터셋 통계  /  Layer 2  RAG Triad  /  Layer 3  품질 평가
```

### 평가 완료 후 수동 이동

- 자동 탭 전환 제거
- "평가 결과 보기" 버튼 → `onGoToEvaluation()` 콜백으로 수동 이동
- `App.tsx`: `onGoToEvaluation={() => setActiveTab("evaluation")}`

### 관련 파일

| 파일 | 변경 내용 |
|------|-----------|
| `QAGenerationPanel.tsx` | 레이아웃 전면 재작성, AnimatePresence 추가, `onGoToEvaluation` prop |
| `App.tsx` | `onGoToEvaluation` 콜백 연결, 자동 탭 전환 제거 |

---

## ✅ 평가 파이프라인 — Syntax Validation 전량 실패 버그 수정 (2026-03-17)

### 원인

`SyntaxValidator`가 요구하는 필드: `q`, `a`, `context`

Supabase 저장 구조:
```
qa_gen_results.qa_list = [
  { docId, hierarchy, text, qa_list: [{q, a, intent, answerable}] }  ← context 없음
]
```

로컬 파일 fallback 경로는 `result["text"]`를 `context`로 주입하는 flatten 로직이 있었으나,
**Supabase 경로는 `qa_list`를 그대로 사용** → `context` 필드 없음 → 전량 실패.

### 수정 — `backend/evaluators/pipeline.py`

```python
# 기존 (Supabase 경로)
qa_list = gen_data.get("qa_list", [])   # raw results 배열을 그대로 사용

# 수정 후 — 로컬 fallback과 동일하게 flatten + context 주입
raw_results = gen_data.get("qa_list", [])
for result_idx, result in enumerate(raw_results):
    context = result.get("text", "")
    for qa_idx, qa in enumerate(result.get("qa_list", [])):
        qa_list.append({
            "q":       qa.get("q", ""),
            "a":       qa.get("a", ""),
            "context": context[:10000],
            "qa_id":   qa.get("qa_id", f"qa_{result_idx}_{qa_idx}"),
            "intent":  qa.get("intent", ""),
            "docId":   result.get("docId", ""),
        })
```

**수정 전**: `Layer 1-A: 0/N passed` (전량 실패)
**수정 후**: `Layer 1-A: 8/8 passed` (정상)

---

## ✅ 터미널 출력 — 이모지 제거 + `[object Object]` 수정 (2026-03-17)

### 문제 1 — `[object Object]` 출력

```typescript
// 기존 (평가 완료 로그)
addLog(`✓ 전체 평가 완료: ${data.eval_report}`, 'success');
// data.eval_report는 객체 → 템플릿 리터럴에서 [object Object]로 변환됨
```

```typescript
// 수정
addLog('전체 평가 파이프라인 완료', 'success');
```

### 문제 2 — 이모지 포함 Layer 이름

```typescript
// 기존
syntax: "1️⃣ 구문 검증",  stats: "2️⃣ 데이터셋 통계",
rag: "3️⃣ RAG Triad 평가",  quality: "4️⃣ 품질 평가"

// 수정
const LAYER_NAMES: Record<string, string> = {
  syntax:  "Layer 1-A  구문 검증",
  stats:   "Layer 1-B  데이터셋 통계",
  rag:     "Layer 2    RAG Triad",
  quality: "Layer 3    품질 평가",
};
```

Backend `pipeline.py`의 `update_job` 메시지도 동일하게 정리:
```
"1️⃣ 구문 검증 중..."   → "Layer 1-A: 구문 검증 중..."
"3️⃣ RAG Triad 평가..."  → "Layer 2: RAG Triad 평가..."
"4️⃣ LLM 품질 평가..."   → "Layer 3: 품질 평가..."
```

---

## ✅ Hierarchy L3 선택 UI 추가 (2026-03-17)

### 목표

Generation 패널 Hierarchy 섹션에서 L3까지 선택 가능하도록 확장.
L3가 존재하는 경우에만 드롭다운 노출.

### Backend — `get_hierarchy_list()` L3 수집 추가

**`backend/config/supabase_client.py`**

```python
# 기존: l2_by_l1만 수집
# 수정: l3_by_l1_l2 추가 (복합키: "L1__L2")

l3_by_l1_l2: Dict[str, set] = {}
for chunk in chunks:
    l1, l2, l3 = meta.get("hierarchy_l1"), meta.get("hierarchy_l2"), meta.get("hierarchy_l3")
    if l1 and l2 and l3:
        key = f"{l1}__{l2}"
        l3_by_l1_l2.setdefault(key, set()).add(l3)

return {
    "l1_list": ...,
    "l2_by_l1": ...,
    "l3_by_l1_l2": {k: sorted(v) for k, v in l3_by_l1_l2.items()},  # ← 신규
}
```

### Frontend 수정

| 파일 | 변경 내용 |
|------|-----------|
| `src/lib/api.ts` | `getHierarchyList()` 반환 타입에 `l3_by_l1_l2` 추가, fallback에도 `{}` 포함 |
| `QAGenerationPanel.tsx` | `hierarchyL3Map`, `selectedL3` state 추가 |
| `QAGenerationPanel.tsx` | `handleL2Change()` — L2 변경 시 L3 초기화 |
| `QAGenerationPanel.tsx` | L3 select: `selectedL2` 있고 해당 L3 목록 존재 시에만 노출 |
| `QAGenerationPanel.tsx` | 생성 payload에 `hierarchy_l3: selectedL3` 포함 |
| `QAGenerationPanel.tsx` | 선택 범위 표시에 L3 항목 추가 (pl-8 들여쓰기) |

### L3 select 노출 조건

```typescript
{selectedL1 && selectedL2 && (hierarchyL3Map[`${selectedL1}__${selectedL2}`] || []).length > 0 && (
  <select value={selectedL3} onChange={(e) => setSelectedL3(e.target.value)} ...>
    <option value="">전체 (L2 내 모든 항목)</option>
    {(hierarchyL3Map[`${selectedL1}__${selectedL2}`] || []).map((l3: string) => ...)}
  </select>
)}
```

### 선택 범위 표시 (3단계)

```
> L1 (indigo-700)
    > L2 (indigo-400, pl-4)
        > L3 (indigo-300, pl-8)  ← 신규
```

---

## ✅ TypeScript Hint 정리 (2026-03-17)

`QAGenerationPanel.tsx` 암시적 `any` 파라미터 힌트 수정:

| 위치 | 수정 내용 |
|------|-----------|
| `hierarchyL1List.map((l1)` | `(l1: string)` |
| `hierarchyL2Map[...].map((l2)` | `(l2: string)` |
| `logs.map((log, i)` | `(log: string, i: number)` |
| `onBlur={(e: React.FocusEvent<...>)` | `(e)` — 컨텍스트에서 추론 가능, React 네임스페이스 에러 방지 |

---

## ✅ PDF 파싱 품질 개선 — 중첩 표 / 체크박스 노이즈 처리 (2026-03-17)

### 배경

테스트데이터_3.pdf page 22~23의 복잡한 중첩 표(체크박스 포함)에서 두 가지 문제 발생:

1. **체크박스 셀 노이즈**: `□`, `⋮` 기호가 `| ⋮ | □ □ □v □ | ⋮ |` 형태의 무의미한 청크로 저장
2. **TOC 오판**: `| 중대한 수준 · 재앙적 · 치명적 |` 표 셀 내용이 가운뎃점 밀도 체크에 걸려 TOC로 필터링됨

### Phase 2.15 — `normalize_text` 기호 정규화

**`backend/api/ingestion_api.py`**

```python
# 체크박스/폼 필드 기호 정규화
text = re.sub(r'[□☐\u25a1\u2610]', '[ ]', text)   # 빈 체크박스 → [ ]
text = re.sub(r'[☑✓✔\u2611\u2713\u2714]', '[v]', text)  # 체크 완료 → [v]

# 중첩 표 수직 생략 기호 제거
text = re.sub(r'[\u22ee\u22ef\u22f0\u22f1]', '', text)  # ⋮ ⋯ ⋰ ⋱
```

### Phase 2.15 — `_is_symbol_noise_chunk` 필터 추가

```python
def _is_symbol_noise_chunk(text: str) -> bool:
    """파이프/공백 제거 후 한글·영문·숫자 비율 15% 미만이면 noise"""
    base = re.sub(r'[\s|]', '', text)
    if not base:
        return True
    meaningful = len(re.findall(r'[가-힣a-zA-Z0-9]', base))
    return len(base) >= 15 and meaningful / len(base) < 0.15
```

청크 처리 루프에서 TOC → Colophon → **Symbol Noise** 순서로 적용.

**결과**: `| ⋮ | ⋮ | □ □ □v □ | ⋮ |` 청크 필터링 확인 (p22)

### Phase 2.16 — `is_toc_chunk` 표 청크 예외 처리

**문제**: Layer 1/3 가운뎃점 밀도 체크가 표 셀 내용(`중대한 수준 · 재앙적 · 치명적`)에 오판

**수정**:

```python
# 표 청크 판별 — '|' 파이프 4개 이상
is_table_content = text.count('|') >= 4

# Layer 1: 표 청크는 건너뜀
if not is_table_content:
    special_dots = text.count('·')
    if special_dots > 10 and special_dots / len(text) > 0.05:
        return True

# Layer 3: 표 청크는 건너뜀
if not is_table_content:
    # 점선 + 페이지번호 라인 비율 체크
    ...
```

Layer 2 (연속 `····` 4개) 는 표 청크에도 적용 유지 — TOC를 표 형태로 만든 케이스 대비.

**결과**: 이전 46 chunks → 수정 후 **47 chunks** (p24 표 청크 복원)

