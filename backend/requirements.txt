# ============= Web Framework =============
fastapi>=0.135.1
uvicorn[standard]>=0.41.0
python-multipart>=0.0.22        # UploadFile / Form 지원
pydantic>=2.12.5
python-dotenv>=1.0.0
aiofiles>=25.1.0

# ============= Database =============
supabase>=2.28.0

# ============= Document Parsing =============
pymupdf>=1.27.2                     # PDF 파싱 (import fitz)
python-docx>=1.1.0                  # DOCX 파싱 (from docx import Document)
langchain-text-splitters>=0.3.0    # RecursiveCharacterTextSplitter

# ============= Utilities =============
numpy>=2.4.3
pandas>=3.0.1
scipy>=1.17.1
requests>=2.32.5
openpyxl>=3.1.5                     # XLSX 내보내기
rich<14.0                           # 콘솔 출력

# ============= LLM Providers (Generation) =============
google-genai>=1.66.0                # Gemini API (임베딩 + 생성)
anthropic>=0.84.0                   # Claude API
openai>=1.0.0                       # GPT API

# ============= LangChain Integrations (Evaluation) =============
langchain-google-genai>=4.2.1       # RAG Triad — Gemini judge model
langchain-anthropic>=1.3.4          # RAG Triad / QA Quality — Claude judge model
langchain-openai>=1.1.9             # RAG Triad / QA Quality — GPT judge model

# ============= Evaluation Framework =============
trulens>=0.13.4                     # TruLens core
trulens-providers-openai>=2.7.0    # TruLens OpenAI provider
trulens-providers-google>=2.7.1    # TruLens Google provider

# ============= 분석 스크립트용 (백엔드 API에서 미사용) =============
matplotlib>=3.10.8
seaborn>=0.13.2
chromadb>=1.5.4
deepeval>=3.8.9
langchain-chroma>=1.1.0
