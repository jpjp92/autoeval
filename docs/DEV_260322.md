# DEV_260322 — Graph RAG 적용 설계

---

## 1. 배경 및 한계

현재 AutoEval은 청크 독립 가정 기반의 flat vector RAG 구조.
문서 간, 청크 간 의미적 관계가 없어 관계형 QA 생성 및 멀티홉 평가가 불가능한 구조적 한계 존재.

| 한계 | 영향 |
|------|------|
| 청크 각각이 독립적 — 청크 간 관계 없음 | 관계형 질문 생성 불가 |
| H1/H2/H3는 분류 체계, 의미적 관계 그래프 아님 | 멀티홉 컨텍스트 구성 불가 |
| Groundedness 평가가 단일 청크 기준 | 문서 내 모순 감지 전무 |

---

## 2. 핵심 결정 로그 (Decision Log)

설계 과정에서 내린 주요 판단과 근거.

| 결정 | 선택 | 근거 |
|------|------|------|
| 저장 방식 | `metadata.graph` JSONB 직접 확장 | POC 범위에서 Neo4j 등 별도 인프라 운영 비용이 이득보다 큼. `update_chunk_metadata()` 기존 경로 재활용으로 리스크 최소화 |
| 온톨로지 방식 | 문서 귀납형 동적 그래프 | 업로드 문서 도메인이 매번 다름. 사전 정의 온톨로지는 범용성 확보 불가 |
| Layer 4 적용 범위 | 조건부 레이어 | graph_claims 없는 QA에 중립값 부여 시 점수 왜곡. 기존 공식 하위 호환 유지가 더 안전 |
| `contradicts` 채우는 시점 | 3단계(평가 전 문서 단위 분석) | 청크 처리 시점(1단계)에는 다른 청크를 알 수 없음. 1단계 스키마에는 빈 배열로만 존재 |
| pairwise 모순 분석 | embedding 필터 후 LLM | 청크 N개 → N(N-1)/2 LLM 호출. 청크 50개 기준 1,225회. 고유사도 필터로 후보 쌍 좁힘 → 섹션 4 비용 절충 참고 |

---

## 3. 설계 상세

### 3-1. Graph RAG 삽입 위치

```
[현재]
Upload → Chunk → Embed → Hierarchy → Generate QA → Evaluate

[Graph RAG 삽입 후]
Upload → Chunk → Embed → Hierarchy
                              ↓
                    apply-granular-tagging
                    + entity/relation 추출   ← 프롬프트 확장
                              ↓
                    metadata.graph 저장
                              ↓
             Generate QA (그래프 컨텍스트 포함)
                              ↓
             Evaluate (+ Graph Consistency 조건부)
```

### 3-2. 저장 전략 — metadata JSONB 직접 확장

```python
# ingestion_api.py:602 — 기존 update_chunk_metadata() 패턴 재활용
meta = {
    **target.get("metadata", {}),
    "hierarchy_h1": ...,
    "hierarchy_h2": ...,
    "graph": {
        "entities": ["환불정책", "14일"],
        "relations": [["환불정책", "조건", "14일"]],
        "contradicts": []   # 1단계에서는 항상 빈 배열 — 3단계에서 채움
    }
}
```

`apply-granular-tagging` 응답 스키마 확장:
```json
// 현재
[{"idx": 0, "hierarchy": {"h1": "...", "h2": "...", "h3": "..."}}]

// 확장
[{
  "idx": 0,
  "hierarchy": {"h1": "...", "h2": "...", "h3": "..."},
  "graph": {
    "entities": ["환불정책", "14일"],
    "relations": [["환불정책", "조건", "14일"]],
    "contradicts": []
  }
}]
```

### 3-3. QA 생성 확장

신규 intent 유형:

| intent | 설명 | 생성 제약 |
|--------|------|-----------|
| `relational` | "A와 B의 관계는?" | `graph.relations`에 두 엔티티 간 경로 존재 시에만 |
| `comparative` | "X 방식과 Y 방식의 차이는?" | 동일 조건 |
| `causal_chain` | 연쇄 인과 관계 | 동일 조건 |

> 그래프 경로 없이 생성 시 평가 단계 근거 추적 불가 → Groundedness 점수 신뢰도 하락.

**이웃 청크 컨텍스트 토큰 상한 (prompts.py 설계 시 확정)**

depth=2 이웃 병합 시 컨텍스트 3~5배 증가. `claude-sonnet-4-6` workers=2로 가장 제약.

```
<context>{original_chunk}</context>
<graph_context max_tokens="{N}">     ← 원본 컨텍스트 토큰의 50% 이하 권고
  {neighbor_chunks_truncated}
</graph_context>
```

### 3-4. 평가 레이어 확장

```
[현재]
Layer 1-A: Syntax Validation      (0.10)
Layer 1-B: Dataset Statistics     (0.10)
Layer 2:   RAG Triad              (0.40)  ← Groundedness 조건부 분기 추가
Layer 3:   Quality                (0.40)

final_score = syntax×0.1 + stats×0.1 + rag×0.4 + quality×0.4

[추가]
Layer 4:   Graph Consistency (조건부)
  - 답변 내 엔티티가 그래프상 실제 연결되는가
  - 관계형 QA 답변이 그래프 경로와 일치하는가
  - 해당 청크에 contradicts 존재 여부 확인
```

**Layer 4 점수 공식 — 조건부 레이어**

```
graph_claims 있는 QA:
  final_score = syntax×0.1 + stats×0.1 + rag×0.35 + quality×0.35 + graph×0.10

graph_claims 없는 QA:
  final_score = syntax×0.1 + stats×0.1 + rag×0.4 + quality×0.4  (기존 유지)
```

`qa_eval_results.scores` JSONB 추가 필드:
```json
"graph": {"applicable": true, "score": 0.88}
```

**Groundedness 조건부 분기 (`rag_triad.py`)**

```python
def evaluate_all_with_reasons(self, question, answer, context, graph_claims=None):
    # graph_claims 있으면 → CoT Step 1 skip, 사전 추출 claims로 검증만
    # graph_claims 없으면 → 현재 CoT 방식 그대로 (하위 호환)
```

---

## 4. 구현 로드맵

| 단계 | 내용 | 구현 위치 | 선행 조건 | 완료 기준 |
|------|------|-----------|-----------|-----------|
| **1단계** | `apply-granular-tagging` 프롬프트 확장 → `metadata.graph` 저장 (entities + relations, contradicts `[]` 고정) | `ingestion_api.py` | — | graph 블록 파싱 성공률(fallback 빈 배열 제외) ≥ 80% — 잠정치, 초기 테스트 실측 후 조정 |
| **1단계-B** | 기존 문서 전체 백필 (`graph` 없는 doc_chunks 재처리) | 일회성 마이그레이션 스크립트 | 1단계 배포 후 | 전체 doc_chunks 중 `metadata.graph` 존재 비율 = 100% |
| **2단계** | Groundedness 조건부 분기 — `graph_claims` 있으면 검증 전용 경로 | `rag_triad.py` | **1단계-B 완료 후 배포** | graph_claims 있는 QA에서 조건부 분기 진입 로그 확인 |
| **3단계** | 평가 전 문서 단위 모순 분석 → `metadata.graph.contradicts` 업데이트 | `evaluators/pipeline.py` 앞단 + `doc_chunk_repo.py` | 1단계-B 완료 | contradicts 비어있지 않은 청크 존재 시 평가 결과에 반영 확인 |
| **4단계** | 관계형 QA intent 추가 + 이웃 청크 컨텍스트 확장 | `generation_api.py` + `prompts.py` | 1단계-B 완료 | relational/comparative/causal_chain intent QA 생성 비율 > 0% |

> **2단계 주의**: `graph_claims 있으면` 분기는 1단계-B 완료 전까지 실제로 타지 않음. 배포 순서 반드시 준수.

### 3단계 쿼리 경로

```
평가 job 시작
  → qa_gen_results.id 로 source_doc(파일명) 조회
  → doc_chunk_repo.get_chunks_by_doc(doc_name)  ← 신규 함수 필요
      (또는 get_chunks_by_ids() + qa_list[*].docId 배열로 범위 한정)
  → pairwise 모순 분석 (아래 비용 절충안 적용)
  → metadata.graph.contradicts 업데이트
  → 평가 루프에서 각 QA context 청크 id로 contradicts 참조
```

**pairwise 분석 비용 절충**

```
청크 수 ≤ 30개  → 전체 pairwise LLM 호출 허용 (최대 435회)
청크 수 > 30개  → 고유사도 필터 선처리 필수
                   cosine similarity ≥ 0.75 인 쌍만 LLM 호출 대상
                   (의미적으로 유사한 청크 간에 모순 발생 가능성 높음)
```

---

## 5. 미결 사항

구현 전 확정이 필요한 항목:

- [ ] **Layer 4 가중치 수치 확정** — 위 0.10 배분은 잠정안. `evaluation_repo.py` 스키마 반영 전 확정 필요
- [ ] **`dashboard_repo.py` 집계 쿼리 영향 범위 확인** — Layer 4 조건부로 같은 job 내 QA마다 `final_score` 계산식이 달라짐. `grade_dist`·`summary` 집계가 현재 `final_score` 단일 값 기준이라 혼합 공식 QA 평균 시 왜곡 발생. Layer 4 가중치 확정과 묶어서 수정 범위 결정 필요
- [ ] **`doc_chunk_repo.py`에 `get_chunks_by_doc(doc_name)` 신규 함수** 추가
- [ ] **이웃 청크 병합 토큰 상한 구체 수치** — "원본의 50% 이하" 권고, 모델별 실측 후 prompts.py에 상수 정의
- [ ] **1단계-B 백필 스크립트 범위** — 전체 문서 vs `doc_name` 기준 선택 처리 여부

---

## 6. 롤백 및 실패 처리

### 롤백 전략

**1단계-B 백필은 파괴적 작업** (`metadata` 직접 수정). 실행 전 확인 사항:

```
graph 키만 추가하는 방식이므로 기존 metadata 키는 보존됨.
원복 방법: UPDATE doc_chunks SET metadata = metadata - 'graph' WHERE ...
→ 별도 컬럼 백업 불필요, 키 삭제로 원복 가능.
```

백필 스크립트 필수 옵션:
- `--dry-run`: 실제 쓰기 없이 대상 청크 수 및 샘플 출력만
- `--doc-name`: 특정 문서만 선택 처리 (전체 일괄 전 검증용)

각 단계 배포 전 체크:
| 단계 | 롤백 방법 |
|------|-----------|
| 1단계 | 프롬프트 이전 버전 복구. 이미 저장된 `graph` 키는 무해함 |
| 1단계-B | `metadata - 'graph'` 쿼리로 전체 원복 |
| 2단계 | `rag_triad.py` 이전 커밋 복구. graph_claims 파라미터 제거 시 기존 동작 그대로 |
| 3단계 | `contradicts` 필드 무시하도록 pipeline 플래그 추가 권고 |
| 4단계 | `generation_api.py`·`prompts.py` 이전 커밋 복구. **단, 이미 생성된 relational/comparative/causal_chain intent QA는 DB에 잔존.** 롤백 후 해당 job의 관계형 intent QA는 평가에서 제외 또는 수동 삭제 처리 필요. 자동 처리 원할 경우 평가 파이프라인에 "graph_claims 없는 relational intent → 평가 skip" 방어 로직 사전 추가 권고. |

### 실패 처리

**1단계 — graph 블록 파싱 실패 시**

LLM이 graph 블록 누락 또는 잘못된 형식으로 반환할 경우:

```python
# fallback: 빈 구조로 저장 (재시도 없음)
# 데이터 불일치 방지를 위해 null 저장 금지
graph_fallback = {"entities": [], "relations": [], "contradicts": []}
```

판단 근거: graph 블록 파싱 실패 → `graph: null` 저장 시 2단계 조건부 분기에서
`graph_claims is None` 체크가 null과 []를 구분하지 못해 조용한 오류 발생 가능.
빈 배열 기본값이 가장 안전.

**2단계 — graph_claims 분기 오동작 시**

`graph_claims=[]`인 QA가 CoT Step 1 skip 경로로 잘못 진입하는 케이스.
`None`과 `[]` 모두 기존 CoT 경로로 처리되도록 방어 코드 필수:

```python
if not graph_claims:  # None과 [] 모두 처리
    # 기존 CoT 방식 그대로
```

배포 후 로그에서 `graph_claims=[]` QA가 skip 경로 진입 여부 확인.

**3단계 — pairwise 분석 중 LLM 오류 시**

해당 쌍 건너뜀, `contradicts`에 미추가. 평가 계속 진행.
오류율 ≥ 20% 시 해당 평가 job에 `graph_analysis_partial: true` 플래그 기록.

**4단계 — 경로 없는 relational intent 생성 시**

`graph.relations` 체크 없이 생성된 관계형 QA는 Layer 2 Groundedness에서
근거 추적 실패 → 낮은 점수로 집계. 데이터 오염은 아니나 점수 왜곡.
생성 시점 경로 체크 로직 누락 여부를 배포 전 단위 테스트로 검증 권고.

---

## 7. 세션 변경 이력 (2026-03-22)

### REF 문서 / README 정합성 수정

| 파일 | 내용 |
|------|------|
| `docs/REF_BACKFLOW.md` | `/api/generation` → `/api/generate`, `/api/evaluation` → `/api/evaluate` 전체 경로 수정. 누락된 `GET /api/evaluate/{job_id}/status` 추가 |
| `docs/REF_FRONTFLOW.md` | API 경로 동일 수정. 실제 미존재 `getGenStatus()` / `getGenPreview()` 제거. direct fetch 주석 추가 |
| `docs/REF_DB.md` | `vector(3072)` → `vector(1536)` (실측치 반영) |
| `README.md` (루트) | 아키텍처 트리 API 경로 주석 수정 |
| `backend/README.md` | `pipeline.py` 중복 항목 제거. `GET /api/generate/{job_id}/preview` 누락 추가 |
| `frontend/README.md` | `HierarchyConstructionPanel.tsx`, `agents/AgentTable.tsx`, `analytics/AnalyticsDashboard.tsx` 디렉토리 구조 반영 |

### 대시보드 파이프라인 로그 리팩토링

**배경**: `recent_jobs`가 생성·평가 행을 독립적으로 나열해 연결 관계 파악 불가. `linked_evaluation_id` FK가 DB에 존재했으나 대시보드 쿼리에서 미사용.

**변경 내용**:

`backend/db/dashboard_repo.py`
- gen 쿼리에 `linked_evaluation_id` 추가
- `eval_map = {e["id"]: e for e in eval_rows}` O(1) 조회 구조
- `recent_jobs` → 생성 행에 연결 평가 결과 인라인(`eval_job_id`, `eval_score`, `eval_grade`)
- eval 전용 행 섹션 제거

`frontend/src/components/dashboard/DashboardOverview.tsx`
- 인터페이스: `type` 제거 → `eval_job_id / eval_score / eval_grade` 추가
- `onEvalSelect?: (evalJobId: string) => void` prop 추가
- 테이블: `유형` 컬럼 제거 → `평가` 컬럼 (등급 배지 or "미평가")
- 평가 연결 행: `cursor-pointer` + click → `onEvalSelect(eval_job_id)`

`frontend/src/App.tsx`
- `onEvalSelect` 연결: `setLastEvalJobId(evalJobId)` + `setActiveTab("evaluation")` 동시 실행

---

## 8. UI 개선 계획 (2026-03-23)

### 8-1. QADetailView 레이아웃 재구성

**문제**

| 문제 | 위치 |
|------|------|
| 점수(avg)와 근거(reason)가 분리 — 같은 정보를 두 섹션에 나눠 표현 | 점수 2열 ↔ 평가 근거 |
| 중첩 컨테이너 3단계 — 평가 근거 div > RAG 박스 > row | 평가 근거 섹션 |
| 주요 실패 유형이 맨 마지막 — 실패 QA 검토 시 가장 먼저 봐야 할 정보 | 전체 구조 |

**개선 후 섹션 순서**

```
[헤더] 목록으로 | #번호 · intent · 상태
[실패 callout]  ← primary_failure 있을 때만, 헤더 직후
[질문]
[답변]
[컨텍스트]
[RAG Triad 섹션]  헤더에 avg 포함 → 차원별 reason 나열
[품질 평가 섹션]  헤더에 avg 포함 → 차원별 reason 나열
```

**구조 변경 요점**

| 현재 | 개선 |
|------|------|
| 점수 2열 카드 + 평가 근거 2박스 = 4개 컨테이너 | RAG 섹션 + 품질 섹션 = 2개 컨테이너 |
| avg 카드 → 스크롤 → reason 박스 (분리) | avg를 섹션 헤더에 인라인 (통합) |
| 주요 실패 유형 맨 마지막 | 헤더 직후로 이동 |
| 중첩 3단계 | 섹션 > row 2단계 |

**수정 파일**: `QAEvaluationDashboard.tsx` — `QADetailView` 함수 내부만 변경

---

### 8-2. 대시보드 로그 클릭 → Evaluation 히스토리 연동 버그

**원인**

```
Dashboard onEvalSelect(eval_job_id)
  → App.tsx: setLastEvalJobId(eval_job_id)
  → QAEvaluationDashboard: evalJobId prop 수신
  → useEffect: getEvalStatus(evalJobId)   ← in-memory 세션 조회만 함
```

`getEvalStatus()`는 현재 세션 in-memory job만 조회. Dashboard에서 넘어오는 `eval_job_id`는 DB 저장 과거 평가라 in-memory에 없음 → 조회 실패 → 마지막 세션 상태 유지.

히스토리 선택 경로(`selectHistory` → `getEvalExportById(item.id)`)가 Dashboard 진입 시 연결되지 않은 것.

추가 문제: `recent_jobs`에서 반환하는 `eval_job_id`는 `qa_eval_results.job_id`(UUID)이지만, `selectHistory`는 `qa_eval_results.id`(Supabase PK)를 필요로 함 → DB에서 애초에 잘못된 식별자를 반환.

**수정 범위**

| 파일 | 변경 내용 |
|------|----------|
| `backend/db/dashboard_repo.py` | `recent_jobs`에 `eval_id` 필드 추가 (`linked_eval.get("id")`) |
| `frontend/.../DashboardOverview.tsx` | 인터페이스 `eval_id` 추가, `onEvalSelect` 시 `eval_id` 전달 |
| `frontend/src/App.tsx` | `lastEvalDbId` state 추가, `initialEvalDbId` prop 연결 (기존 `lastEvalJobId`는 Generation 세션용으로 유지) |
| `frontend/.../QAEvaluationDashboard.tsx` | `initialEvalDbId` prop 추가. historyList 로드 후 id 매칭 → `selectHistory` 자동 호출 |

**effect 분리 설계**

```
// historyList 로드 (마운트 시 1회)
useEffect(() => { getEvalHistory() → setHistoryList }, []);

// initialEvalDbId 변경 시 자동 선택 (historyList 준비 대기)
useEffect(() => {
  if (!initialEvalDbId || !historyList.length) return;
  const target = historyList.find(h => h.id === initialEvalDbId);
  if (target) selectHistory(target);
}, [initialEvalDbId, historyList]);
```

`historyList` 로드와 `initialEvalDbId` 설정 타이밍을 effect 분리로 해결. historyList가 먼저 로드되든 나중에 로드되든 두 조건이 모두 충족되면 자동 선택.

---

## 9. 구현 완료 및 이슈 해결 (2026-03-23)

### 9-1. 8-1 QADetailView 레이아웃 — 구현 완료

`QAEvaluationDashboard.tsx`의 `QADetailView` 함수 재작성.

**변경 요점**

| 항목 | 이전 | 이후 |
|------|------|------|
| 실패 유형 위치 | 평가 근거 섹션 맨 마지막 | 헤더 직후 (callout) |
| 점수 + 근거 구조 | 점수 2열 카드 + 평가 근거 2박스 = 4컨테이너 | RAG Triad 섹션 + 품질 평가 섹션 = 2컨테이너 |
| avg/reason 관계 | avg 카드 → 스크롤 → reason 박스 (분리) | avg를 섹션 헤더에 인라인 (통합) |
| 중첩 depth | 평가 근거 div > 박스 > row (3단계) | 섹션 > row (2단계) |
| avg 색상 기준 | 2단계 (≥0.7 / 미만) | 3단계 (≥0.85 emerald / ≥0.7 amber / <0.7 rose) |

---

### 9-2. 8-2 대시보드 → Evaluation 히스토리 연동 — 구현 완료

**실제 원인 (계획과 차이)**

계획 수립 시 `linked_evaluation_id`가 DB에서 NULL이라 가정했으나, 실제로는 **정상 저장돼 있었음**. `--reload` 모드가 이전 세션의 `dashboard_repo.py` 변경사항(linked_evaluation_id SELECT 추가, eval_map 구조 변경)을 모듈 캐시로 인해 반영하지 못한 것이 원인. 풀 재시작 후 정상 동작 확인.

**구현 내용**

`backend/db/dashboard_repo.py`
- `eval_id` 필드 추가 (`qa_eval_results.id` Supabase PK)
- 폴백 매칭 추가: `eval.metadata.generation_id == gen.id` (FK 미설정 레코드 대비)

`frontend/src/components/dashboard/DashboardOverview.tsx`
- 인터페이스 `eval_id` 추가, 클릭 시 `eval_id` 전달

`frontend/src/App.tsx`
- `lastEvalDbId` state 신규 추가 (기존 `lastEvalJobId`는 Generation 세션 in-memory용 유지)
- `onEvalSelect` → `setLastEvalDbId` 연결

`frontend/src/components/evaluation/QAEvaluationDashboard.tsx`
- `initialEvalDbId` prop 추가
- historyList 로드 후 id 매칭 → `selectHistory` 자동 호출 (effect 분리로 타이밍 문제 해결)

**진단 과정 메모**

- 초기 증상: 대시보드 로그 전체 "미평가", 행 클릭 불가
- 진단 로그 추가 후 확인: `gen.linked_evaluation_id == eval_map keys` → FK 매칭 정상
- 원인: `--reload`가 dashboard_repo.py 모듈 변경을 반영하지 못함
- 해결: 백엔드 풀 재시작
- 진단 로그는 확인 후 제거

---

### 9-3. QADetailView 섹션 레이블 통일 + 평가 색상 구분

`QAEvaluationDashboard.tsx` — `QADetailView` 추가 수정.

| 항목 | 이전 | 이후 |
|------|------|------|
| 질문/답변/컨텍스트 레이블 크기 | `text-[10px] tracking-widest` | `text-xs tracking-wide` (RAG·품질과 동일) |
| RAG Triad 섹션 배경/테두리 | `bg-white border-slate-200` | `bg-sky-50/40 border-sky-200` |
| RAG Triad 헤더 텍스트 | `text-slate-500` | `text-sky-600` |
| RAG Triad 행 구분선 | `divide-slate-100` | `divide-sky-100` |
| 품질 평가 섹션 배경/테두리 | `bg-white border-slate-200` | `bg-violet-50/40 border-violet-200` |
| 품질 평가 헤더 텍스트 | `text-slate-500` | `text-violet-600` |
| 품질 평가 행 구분선 | `divide-slate-100` | `divide-violet-100` |

---

### 9-4. 대시보드 파이프라인 로그 등급 배지 폭 고정

`DashboardOverview.tsx` — 평가 등급 배지(A+, A, B+, B, C, F) 너비 통일.

`px-2` → `w-8 justify-center` : 고정 32px 폭으로 모든 등급 동일 크기.

---

### 9-5. PipelineFlow.tsx 오류 수정

`frontend/src/components/settings/PipelineFlow.tsx` 검토 결과:

| 위치 | 이전 | 수정 |
|------|------|------|
| STEP 2 DB 노드 레이블 | `doc_chuncks.metadata` (오타) | `doc_chunks.metadata` |
| STEP 4 Layer 3 sub 텍스트 | `사실성 · 완결성` (2개만 표기) | `사실성 · 완전성 · 구체성 · 간결성` (실제 4개 차원 반영) |

전체 5단계 플로우(규격화 → 계층 태깅 → QA 생성 → 4레이어 평가 → 결과 확인) 구조 및 엣지 연결은 현재 코드와 일치. Graph RAG(Layer 4 포함)는 미구현 상태로 다이어그램에 미반영 — 구현 후 별도 추가 예정.

---

## 10. UI 추가 개선 (2026-03-23)

### 10-1. Export — ZIP 번들 추가

`exportUtils.ts` + `QAEvaluationDashboard.tsx`

- `jszip` 패키지 설치
- `buildWorkbook(data)` 내부 함수 분리 (`exportToCSV`에서 workbook 빌드 로직 추출)
- `buildHTMLContent(data)` 내부 함수 분리 (`exportToHTML`에서 HTML 문자열 빌드 추출)
- `exportToZip(data)` async 함수 추가 — XLSX + HTML 두 파일을 `evaluation-report-YYYY-MM-DD.zip`으로 묶어 다운로드
- Export 드롭다운에 `ZIP` 항목 추가 (XLSX + HTML 묶음)

---

### 10-2. History / Export 드롭다운 — click-outside 닫기

기존 `onMouseLeave` 방식은 커서가 조금만 벗어나도 즉시 닫히는 문제. **click-outside 패턴**으로 교체.

| 드롭다운 | 변경 내용 |
|----------|-----------|
| Export | 메인 컴포넌트에 `exportMenuRef` + `useEffect(mousedown handler)` 추가, `onMouseLeave` 제거 |
| History | `HistoryDropdown` 내부에 `ref` + `useEffect(mousedown handler)` 추가, `onMouseLeave` 제거 |

메뉴 열릴 때만 `document.addEventListener` 등록 → 닫히거나 unmount 시 자동 정리.

---

### 10-3. 의도 분류 차트 헤더 아이콘 통일

**React 컴포넌트** (`QAEvaluationDashboard.tsx`)

기존 `🗂️` 이모지를 Lucide `LayoutGrid` 아이콘으로 교체. 세 차트 헤더 모두 `flex items-center gap-2 + Lucide 아이콘` 구조로 통일.

| 차트 | 아이콘 | 색상 |
|------|--------|------|
| 의도 분류 | `LayoutGrid` | `text-cyan-500` |
| 데이터 통계 | `Zap` | `text-amber-500` (기존 유지) |
| RAG Triad + 품질 평가 | `Target` | `text-indigo-500` (기존 유지) |

**HTML 리포트** (`exportUtils.ts`)

이모지(🗂️ / ⚡ / 🎯) 전체 제거 → 인라인 SVG 아이콘으로 교체. React 컴포넌트와 동일한 아이콘/색상 적용.

---

### 10-4. HTML 리포트 개선

**`exportUtils.ts` 변경 요점**

| 항목 | 변경 내용 |
|------|-----------|
| QA 상세 테이블 | 정적 table → JS 기반 페이지네이션(5개/페이지, 이전·다음 버튼) |
| QA 상세 클릭 | 행 클릭 → 세부 페이지(질문·답변·컨텍스트·RAG Triad reasons·품질 평가 reasons) |
| 세부 페이지 헤더 | `← 목록으로` + `#ID · 의도 · 상태` (점수 표기 제거) |
| 세부 페이지 스타일 | 평가 페이지와 동일: 질문(slate) / 답변(indigo) / 컨텍스트(amber) / RAG Triad(sky) / 품질 평가(violet) |
| 평가 실패 callout | 세부 페이지에서 제거 |
| RAG Triad 지표 | 관련성 · 근거성 · **명확성** (명확성은 RAG 그룹) |
| 품질 평가 지표 | 사실성 · 완전성 · 구체성 · 간결성 |
| 데이터 통계 테이블 | 삭제 (차트로 충분) |
| RAG Triad + 품질 평가 테이블 | 삭제 (차트로 충분) |
| svgBars 범례 | 우측 3항목 균등 배치 (겹침 수정), W 280→340 확장 |
| svgDonut 범례 | 차트와 간격 확대 (`ly` 178→194, H 174→190) |
| svgRadar | SVG 높이 226→246, 통합점수 텍스트 하단 여백 추가 |
