# 프론트엔드 리팩토링 계획 (2026-04-06)

## 1. 전역 아키텍처 및 시스템 기반
- [ ] **라우팅 시스템 도입**
    - 현황: `App.tsx`에서 `activeTab` 상태를 사용하여 수동으로 화면 전환.
    - 목표: `react-router-dom` 또는 `TanStack Router`를 도입하여 URL 기반 내비게이션 및 딥 링크 지원.
- [ ] **상태 관리 최적화**
    - 현황: `App.tsx`에 집중된 상태와 복잡한 Prop Drilling 발생.
    - 목표: **Zustand** 또는 **React Context**를 도입하여 테마, 알림, 세션 데이터를 전역에서 관리.
- [ ] **공통 UI 컴포넌트 추출**
    - 현황: 인라인 스타일링과 반복되는 디자인 패턴이 코드 곳곳에 산재.
    - 목표: 버튼, 카드, 입력 폼 등 재사용 가능한 요소를 `src/components/ui`로 공통 컴포넌트화.

## 2. 대형 파일 분리 및 책임 분산
- [ ] **`src/lib/exportUtils.ts` (~867라인) 분리**
    - 내보내기 포맷별로 로직 분리: `xlsxExporter.ts`, `htmlExporter.ts`, `zipExporter.ts`.
    - 하드코딩된 대규모 HTML/SVG 템플릿 문자열을 별도 모듈이나 빌더 패턴으로 추출.
- [ ] **`src/components/evaluation/QAEvaluationDashboard.tsx` (~765라인) 리팩토링**
    - 데이터 페칭 및 필터링 로직을 커스텀 훅(`useEvaluationDashboard`)으로 추출.
    - 요약 통계, 차트 섹션, 상세 리스트 테이블 등으로 컴포넌트 잘게 쪼개기.
- [ ] **`src/components/standardization/DataStandardizationPanel.tsx` (~605라인) 기능 단위 분리**
    - 파일 업로드, 분석 결과 뷰, 계층 구조 트리 뷰를 각각 독립된 컴포넌트로 분리.

## 3. 데이터 통신 및 타입 안정성
- [ ] **API 클라이언트 중앙화**
    - 현황: `api.ts`에서 `fetch`를 직접 사용하여 일관된 에러 처리가 어려움.
    - 목표: Axios 인스턴스 또는 공통 Wrapper를 생성하여 에러 핸들링 및 베이스 URL 설정 일관화.
- [ ] **서비스 로직 통합**
    - 여러 곳에 흩어진 `fetchWithRetry` 로직을 공통 API 서비스로 통합.
- [ ] **런타임 타입 검증 도입**
    - **Zod**를 사용하여 API 응답 데이터의 스키마를 검증함으로써 프론트-백엔드 간 타입 불일치 방지.

## 4. 코드 유지보수 및 최적화
- [ ] **의존성(Dependency) 정리 (`package.json`)**
    - 프론트엔드 빌드에 불필요한 라이브러리(예: `better-sqlite3`, `express`, `dotenv`, `tsx`)를 삭제하거나 `devDependencies`로 이동.
- [ ] **Tailwind 클래스 가독성 개선**
    - 복잡한 조건부 클래스는 `cva` (Class Variance Authority) 등을 활용해 추상화.
- [ ] **자산(CSS/SVG) 관리 최적화**
    - JS/TS 파일 내부에 포함된 대규모 인라인 CSS 및 SVG 문자열을 별도 파일로 분리하여 코드 가독성 확보.
