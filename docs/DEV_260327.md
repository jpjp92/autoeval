# DEV_260327 — LLM 기반 청킹 테스트 결과

## 배경

기존 rule-based 청킹(`chunk_blocks_aware`)이 생성한 청크에서 QA 품질 문제 발생:
- ο (U+03BF Greek omicron) 불릿 잔존으로 문장 흐름 단절
- `y` 문자 (폰트 인코딩 오류 → 불릿 기호) 오염
- 글자 사이 공백 (`산 업 자 원 부`) 미복원
- 단일 PDF 블록에 복수 섹션 헤더 혼합 → 청크 경계 혼란
- 목차 페이지(p12 이후) TOC 필터 미적용으로 불필요한 청크 생성

→ **Gemini 2.5 Flash를 직접 활용한 LLM 청킹으로 전환 테스트** 실시

---

## 구현: `test/chunk_with_llm.py`

### 파이프라인

```
PDF
 └─ extract_text_by_page()         # rawdict + _span_text_with_spaces (공백 복원)
     └─ 원시 블록 목록 (page, y0, text)
         └─ LLM 청킹 (배치 단위)
             ├─ CTX 블록: 이전 배치 마지막 N개 (문맥 참조용, 출력 제외)
             └─ NEW 블록: 이번 배치 청킹 대상
                 └─ merge_short_chunks()     # <100자 청크 인접 청크에 흡수
                     └─ deduplicate_chunks() # Jaccard>0.5 중복 제거
```

### 핵심 설계 결정

| 항목 | 결정 | 이유 |
|------|------|------|
| 공백 복원 | `parsers.extract_text_by_page()` 재사용 | rawdict + `_span_text_with_spaces` 로 40% 임계값 기반 한글 공백 복원 |
| 노이즈 제거 | LLM 프롬프트 규칙으로 처리 | ο, y, Smart, •, Ÿ 등 불릿 패턴 기술 → rule 함수 없이 제거 |
| 배치 overlap | CTX/NEW 태깅 방식 | 이전 배치 마지막 3블록을 CTX로 전달, stride=batch_size로 NEW 중복 없음 |
| 제목 통합 | LLM 프롬프트 규칙 | `제목\n내용` 형식으로 독립 헤더 청크 방지 |
| 짧은 청크 처리 | `merge_short_chunks(min=100)` | LLM이 100자 미달 생성 시 인접 청크에 흡수 (반복 수렴) |
| 중복 제거 | Jaccard(source_blocks) > 0.5 | 배치 경계에서 동일 블록 참조 청크 제거 |

### LLM 프롬프트 주요 규칙

프롬프트는 XML 태그 방식으로 구조화 (`<role>`, `<input_format>`, `<noise_correction>`, `<chunking_rules>`, `<section_title_rules>`, `<output_format>`):

- `<role>`: 범용 청킹 전문가 (문서 유형 무관)
- `<input_format>`: `[CTX-N]` 참고용, `[NEW-N]` 청킹 대상
- `<noise_correction>`: 문장 단절, 불릿 제거, 글자 공백, 숫자·단위 분리, 구두점 위치
- `<chunking_rules>`:
  - 제목 블록(Ⅰ Ⅱ □ 1. 1.1 1단계 등) → 다음 청크 첫 줄로 통합
  - 최소 **200자**, 최대 **600자** (의미 단위 보존 우선)
  - 청크 끝맺음: 접속사·연결어(또한, 그러나, 따라서 등)로 끝나는 경우 다음 청크 앞으로 이동
- `<section_title_rules>`: 청크 전체 내용 기반 핵심 주제 15자 이내 요약
- `<output_format>`: 순수 JSON 배열, 마크다운 감싸기 금지, source_blocks에 NEW 인덱스만 기록

---

### 청크 출력 필드 (chunk response body)

```json
{
  "chunk_index": 4,
  "text": "광역 감시/제어 시스템 개발 목적\n본 사업은 전력계통의...",
  "section_title": "광역 감시/제어 시스템 개발 목적",
  "source_blocks": [10, 11, 12, 13],
  "page": 4
}
```

- `section_title`: LLM이 컨텍스트 기반 요약 추출 (15자 이내), 누락 시 chunk 첫 줄 fallback
- `page`: source_blocks 첫 번째 블록의 page 번호
- `source_blocks`: NEW 블록 인덱스만 (CTX 제외)

---

## 테스트 결과 (3개 문서, pages 1–11)

| 파일 | 원시 블록 | 최종 청크 | avg | min | max | noise | fallback |
|------|-----------|-----------|-----|-----|-----|-------|----------|
| 테스트데이터_8.pdf (p3-11) | 123 | 32 | 176자 | 101 | 321 | 0 | 0 |
| 테스트데이터_5.pdf (p1-10) | 107 | 46 | 194자 | 107 | 345 | 0 | 0 |
| 개인정보보호법 (p1-10) | 218 | 57 | 221자 | 104 | 441 | 0 | 0 |

**noise**: ο·y 등 불릿 기호 잔존 청크 수
**fallback**: JSON 파싱 실패 시 원문 그대로 저장된 청크 수

### 문서 유형별 특성

**테스트데이터_8** (전력계통 연구보고서)
- 연구보고서 특유의 목적·내용·결론 구조 잘 분할
- 세부 목록(1단계/2단계 개발 항목)을 의미 단위로 묶음
- block 0 (`요 약 문목 적 .\nⅠ`) 복합 헤더는 첫 청크에 통합

**테스트데이터_5** (추론형 AI 데이터 품질관리 보고서)
- CHAPTER3, 표 마커 자연스럽게 흡수
- 영문 용어(GIGO, Hallucination, ISO/IEC 5259) + 한글 혼용 처리 깔끔
- 정확성·공정성·효율성 등 테마별로 일관되게 분할

**개인정보보호법** (법령 문서)
- 조항(제1조~제3조) 단위 자연스러운 분할
- 제3조(보호원칙) 7개 항 → 441자, max 500자 설정으로 수용 가능
- cover page 담당부서 연락처 블록 2개가 별도 청크로 분리 (메타 정보)

---

## 기존 rule-based 대비 개선

| 문제 | 기존 | LLM 청킹 |
|------|------|-----------|
| ο 불릿 잔존 | 존재 | 제거됨 |
| y 불릿 잔존 | 존재 | 제거됨 |
| 글자 공백 (`산 업`) | 복원 미흡 | LLM이 수정 |
| 단편 헤더 청크 | 다수 발생 | 통합됨 |
| 100자 미만 청크 | 발생 | post-merge로 흡수 |
| TOC 페이지 유출 | 발생 (p>2 미필터) | LLM이 문맥 파악으로 자연 배제 |

---

## 평가 스코어 공식 변경 (2026-03-27)

### 변경 이유
품질 평가가 완전성(completeness) 단일 지표로 축소됨에 따라 기존 곱셈형 게이트 방식이 completeness에 과도한 영향력 부여 → additive 방식으로 전환 및 비율 재조정.

### 변경 내용

| 레이어 | 이전 | 변경 |
|--------|------|------|
| Syntax | 10% | **5%** |
| Stats | 10% | **5%** |
| RAG Triad | ~80% (completeness 곱셈 게이트) | **65%** (독립 가중) |
| Completeness | 곱셈 게이트 | **25%** (독립 가중) |

```python
# 이전 (곱셈 게이트)
final_score = syntax*0.1 + stats*0.1 + (rag_avg * quality_avg) * 0.8

# 변경 (additive)
final_score = syntax*0.05 + stats*0.05 + rag_avg*0.65 + quality_avg*0.25
```

**점수 비교 예시**

| 케이스 | rag | completeness | 이전 점수 | 변경 점수 |
|--------|-----|-------------|-----------|-----------|
| 우수 | 0.87 | 1.00 | 0.796 | 0.916 |
| 완전성 낮음 | 0.87 | 0.60 | 0.618 | 0.816 |
| RAG 낮음 | 0.65 | 0.90 | 0.568 | 0.690 |

---

## 남은 과제

1. **커버페이지 메타 블록 필터링**: 법령 문서의 담당부서 연락처, 시행일자 등을 ingestion 전 skip 처리 또는 별도 metadata로 저장
2. **ingestion_api.py 통합**: 현재 `parsers.py` rule-based 청킹 대신 LLM 청킹 옵션 추가
3. **전체 문서 테스트**: 1-10p 이상 전체 페이지 대상 배치 안정성 검증
4. **속도 최적화**: 배치당 30-150초 소요 → 실 서비스 적용 시 비동기 처리 필요

---

## 예상 비용 (Gemini 2.5 Flash Paid Tier)

### 단가
| 항목 | 단가 |
|------|------|
| 입력 (text/image/video) | $0.30 / 1M tokens |
| 출력 (thinking tokens 포함) | $2.50 / 1M tokens |

### 배치당 비용
| 항목 | 토큰 수 | 비용 |
|------|---------|------|
| 입력 (SYSTEM_PROMPT + 30블록) | ~2,000 tok | ~$0.0006 |
| 출력 (청크 JSON) | ~1,500 tok | ~$0.0038 |
| **배치 합계** | | **~$0.0044** |

> ⚠️ thinking 토큰이 출력 가격에 포함되므로 실제는 1.5~2배 수준일 수 있음

### 문서별 예상 비용
| 문서 유형 | 블록 수 | 배치 수 | 예상 비용 |
|-----------|---------|---------|-----------|
| 소형 (10p, ~50블록) | 50 | 2 | ~$0.009 |
| 중형 (30p, ~120블록) | 120 | 4~5 | ~$0.018~0.022 |
| 대형 (100p, ~300블록) | 300 | 10 | ~$0.044 |
| 법령 문서 (100p, ~220블록) | 220 | 8 | ~$0.035 |

**Free Tier 사용 시 무료** (일일 한도 내)

### 스크립트 변경 이력

| 날짜 | 버전 | 변경 내용 |
|------|------|-----------|
| 2026-03-27 | v1 | parsers.py 재사용, CTX/NEW 태깅, Jaccard dedup |
| 2026-03-27 | v2 | `merge_short_chunks` post-processing 추가 (min 200자) |
| 2026-03-27 | v3 | `asyncio` + `Semaphore(3)` 병렬 처리, `--parallel` 옵션 |
| 2026-03-27 | v4 | 출력에 `section_title`(컨텍스트 기반 요약), `page` 필드 추가 |
| 2026-03-27 | v4 | `rich` Progress 적용 (진행상황 시각화) |
| 2026-03-27 | v4 | 청크 크기 min 200자 / max 600자로 조정 (QA 생성 품질 개선) |
| 2026-03-27 | v5 | `trim_dangling_conjunctions` 추가 — 접속사·연결어로 끝나는 청크 후처리 |
| 2026-03-27 | v5 | SYSTEM_PROMPT를 XML 태그 구조로 재작성 (`<role>`, `<noise_correction>` 등) |
| 2026-03-27 | v5 | USER_TEMPLATE 입력 블록을 `<blocks>` XML 태그로 구조화 |

---

## Gemini 2.5 Flash Rate Limits

| 항목 | 한도 |
|------|------|
| RPM | 1,000 |
| TPM | 1,000,000 (1M) |
| RPD | 10,000 |

배치당 1회 요청 기준으로 병렬 실행 시 rate limit 초과 없음. 다만 병렬 실행 시 LLM 응답 비결정성(non-determinism)으로 청크 수가 달라질 수 있음 (57개 vs 64개 확인) — 순차 실행이 더 일관된 결과 제공.

---

## 출력 파일

```
test/output/
  chunks_llm_테스트데이터_8_20260327_065929.json
  chunks_llm_테스트데이터_5_20260327_070656.json
  chunks_llm_개인정보_보호법(법률)(제21445호)(20270701)_20260327_070656.json
```

## 관련 스크립트

- `test/chunk_with_llm.py` — LLM 청킹 테스트 스크립트
- `test/inspect_chunking.py` — rule-based 파이프라인 단계별 검사 스크립트
- `backend/ingestion/parsers.py` — 원시 블록 추출 (`extract_text_by_page`)
