# DEV_260405 — 평가 결과 분석 (results7, results8)

## 작업 내용

### P1 적용 (`backend/generators/prompts.py`)
- comparison intent 상한 40% (최대 3개) 제한 추가
- how intent 1개 이상 포함 권장 규칙 추가
- 적용 위치: `SYSTEM_PROMPT_KO_V1`, `SYSTEM_PROMPT_EN_V1`, `build_user_template()` (3곳)

### P2 적용 (`backend/generators/prompts.py`)
- comparison 유형 답변에 수치 나열만 아닌 변화 방향(순위 상승·이탈, 비중 증가/감소 등) 서술 필수 규칙 추가
- 단, 컨텍스트에 방향 미명시 시 추론 금지
- 적용 위치: `SYSTEM_PROMPT_KO_V1` `[comparison형 답변 — 변화 방향 서술 필수]`, `SYSTEM_PROMPT_EN_V1` `[Comparison-type answers — change direction required]` (2곳)

---

## results7 분석 (라면 수출 통계 문서)

> 파일: `docs/eval_sample/results7.json` | 총 QA: 58개 | P1/P2 적용 **전** 생성 결과

### 핵심 지표

| 지표 | 값 |
|------|-----|
| RAG avg_score | 0.987 |
| RAG groundedness | 0.976 (임계 0.8 대비 3.45% 미달 항목 존재) |
| Quality pass_rate | 94.83% (55/58 pass) |
| Quality 실패 | qa#9 (0.60), qa#14 (0.50), qa#39 (0.50) |

### Intent 분포

| intent | 건수 | 비율 |
|--------|------|------|
| comparison | 25 | **43.1%** → P1 상한 초과 |
| fact | 11 | 19.0% |
| list | 8 | 13.8% |
| condition | 7 | 12.1% |
| purpose | 4 | 6.9% |
| how | 3 | **5.2%** → P1 목표 미달 |

### 평가
- comparison 비율이 40% 상한을 초과 — P1 프롬프트 규칙 적용 전 상태
- how 3건은 **정상 동작**: 라면 수출 통계 문서에는 절차·방법 컨텍스트가 없어 how 생성 자체가 부적절한 문서 유형

---

## results8 분석 (개인정보 보호법 문서)

> 파일: `docs/eval_sample/results8.json` | 총 QA: 137개 | P1/P2 적용 **후** 생성 결과

### 핵심 지표

| 지표 | results7 | results8 | 변화 |
|------|---------|---------|------|
| RAG avg_score | 0.987 | 0.979 | -0.008 |
| RAG groundedness | 0.976 | 0.970 | -0.006 |
| Quality pass_rate | 94.83% | 90.51% (124/137 pass) | -4.32%p |
| avg_quality | — | 0.944 | — |

### Intent 분포

| intent | results7 | results8 | 변화 |
|--------|---------|---------|------|
| comparison | 25건 (43.1%) | **18건 (13.1%)** | ✅ P1 효과 확인 |
| how | 3건 (5.2%) | **19건 (13.9%)** | ✅ P1 효과 확인 |
| condition | 7건 (12.1%) | 36건 (26.3%) | ↑ 법률 도메인 특성 |
| fact | 11건 | 22건 | — |
| list | 8건 | 25건 | — |
| purpose | 4건 | 17건 | ↑ |

**→ P1 효과 명확히 확인**: comparison 43% → 13%, how 5% → 14%

### Quality 실패 패턴 (13건)

#### 패턴 1 — Hallucination / Faithfulness (가장 심각)

| qa# | 점수 | 주요 원인 |
|-----|------|----------|
| **#79** | **0.00** | "심의·의결의 목적"을 묻는데 조문에 목적 미명시 → 전부 추론 |
| #76 | fail | 교류·협력 목적 추론 → hallucination |
| #78 | 0.50 | 절차적 관계("선행하고", "병행하여") 추론 |
| #91 | fail | "원활하고 전문적인" 수식어 추론 |

#### 패턴 2 — 답변 누락 (retrieval_miss / 불완전 서술)

| qa# | 점수 | 누락 내용 |
|-----|------|----------|
| #40 | 0.30 | 국가/지방자치단체 책무 5가지 중 1가지만 서술 |
| #54 | 0.40 | 위원장 내부 직무만, 국회/국무회의 외부 직무 누락 |
| #14 | 0.40 | 중앙행정기관 소속 요건 오해석 |
| #66 | 0.50 | 정치활동 금지만 언급, 겸직 금지 누락 |
| #82 | 0.60 | 국외 이전 중지 명령, 의견제시 누락 |
| #130 | 0.50 | 지원 방법 2가지만, 나머지 3가지 누락 |
| #107 | 0.50 | 침해요인 평가 검토 핵심 내용 불충분 |

---

## 종합 평가

### 긍정
- **P1 효과 극적으로 확인** — comparison/how 분포 정상화, diversity 점수 개선 (7.68)
- 중복 QA 0건 유지
- syntax 오류 0건

### 이슈 및 다음 작업 방향

#### 1. Quality 하락 원인 — 법률 문서 purpose 타입 과생성 문제
- 법령 조문은 목적이 명시되지 않는 경우가 많은데, purpose 타입이 17건 생성
- "조문에 목적 미명시" 상태에서 purpose 답변을 생성하면 hallucination 발생
- **P3 검토**: `purpose` 생성 조건 강화 — *컨텍스트에 '~을 목적으로', '~을 위하여' 등 명시적 목적 문구가 있는 경우에만 purpose 생성* 규칙 추가 검토

#### 2. P2 효과 미검증
- results8은 법률 문서로 비교형 수치 변화가 없어 P2(comparison 변화 방향 서술) 효과 확인 불가
- **다음 테스트**: 통계/현황 문서(라면 수출 등)에서 P1+P2 적용 후 재생성 필요

#### 3. 카테고리 "전체"만 표시 버그 (법률 문서 업로드 후 QA Pipeline)
- **원인**: 새로고침 버튼 → `loadHierarchyList()` → `filter_for_qa=true` → `MIN_CHUNKS_FOR_QA=2` 조건
  - 법률 문서는 조문 단위 청크 (1개/~200자)로 필터 미통과 → h1_list 비움
- **연관 파일**:
  - `backend/db/hierarchy_repo.py` — `MIN_CHUNKS_FOR_QA = 2` (line 15)
  - `frontend/src/components/generation/QAGenerationPanel.tsx` — `loadHierarchyList()` line 503
- **수정 방안 후보**:
  - `MIN_CHUNKS_FOR_QA = 1`로 변경 (법령 특성상 타당)
  - 또는 새로고침 시 `filter_for_qa=false`로 호출하고 짧은 컨텍스트 보호 로직 별도 처리

---

## 강화 필요 항목 (우선순위순)

### P3 — Purpose 생성 조건 강화 (Hallucination 방지)
- **문제**: purpose 타입 QA 생성 시 컨텍스트에 목적이 명시되지 않아도 추론하여 생성 → hallucination
  - qa#79 (0점): "심의·의결의 목적"이 법조문에 없음에도 답변 생성
  - qa#76, #78, #91: 동일 패턴
- **강화 방향**:
  - `purpose` 생성 조건: 컨텍스트에 `~을 목적으로`, `~을 위하여`, `~하는 것을 목적`, `in order to`, `for the purpose of` 등 **명시적 목적 문구가 있는 경우에만** 허용
  - 시스템 프롬프트 `<answer_rules>` 또는 `<intent_definitions>` 에 조건 추가
- **적용 파일**: `backend/generators/prompts.py` — `SYSTEM_PROMPT_KO_V1`, `SYSTEM_PROMPT_EN_V1`

### P4 — Faithfulness 강화 (추론성 수식어 금지)
- **문제**: 답변이 컨텍스트를 벗어난 추론성 표현 포함 → groundedness/faithfulness 감점
  - qa#78: "선행하고", "이를 근거로 병행하여" (절차 관계 추론)
  - qa#91: "원활하고 전문적인" (수식어 추론)
  - qa#80: "위원회 안건으로 상정하여" (절차 추론)
- **강화 방향**:
  - 기존 `컨텍스트에 없는 정보 추론 금지` 규칙을 구체화
  - *"컨텍스트에 명시된 단어와 표현만 사용. 절차, 순서, 인과관계, 수식어를 컨텍스트 외에서 추론하지 말 것"* 추가
- **적용 파일**: `backend/generators/prompts.py` — `<constraints>` 섹션

### P5 — List 타입 완전성 강화 (누락 방지)
- **문제**: list 타입 QA에서 컨텍스트에 열거된 항목을 일부 누락하여 답변
  - qa#40 (0.30): 국가/지방자치단체 책무 5가지 중 1가지만 서술
  - qa#54 (0.40): 위원장 직무 내부만 서술, 외부(국회/국무회의) 누락
  - qa#82 (0.60): 사후 조치 목록 일부 누락
  - qa#130 (0.50): 지원 방법 5가지 중 2가지만 서술
- **강화 방향**:
  - `list` 타입 답변 규칙: *"컨텍스트에 '각 호', '다음과 같다', '다음 각 호' 등 열거 표현이 있는 경우 모든 항목을 빠짐없이 포함"* 규칙 추가
  - 질문 생성 시에도 "몇 가지 항목 중 하나" 형태보다 "모든 항목" 형태로 유도
- **적용 파일**: `backend/generators/prompts.py` — `<answer_rules>` 섹션

### P6 — Comparison 타입 변화 방향 서술 (P2 재검증)
- **문제**: results8은 법률 문서로 수치 비교 컨텍스트 없어 P2 효과 검증 불가
- **강화 방향**:
  - 통계/수치 문서 (라면 수출 통계 등)로 재생성하여 comparison 답변에 변화 방향 표현 포함 여부 확인
  - 기대: "2023년 대비 2024년 수출액이 증가하였으나 비중은 감소" 형태의 방향 서술 포함 여부
- **검증 대상**: 다음 라운드 results 파일

### 버그 — 카테고리 필터 (법률 문서 대응)
- **문제**: 법률 문서 업로드 후 QA Pipeline 카테고리 새로고침 시 "전체"만 표시
- **원인**: `filter_for_qa=true` + `MIN_CHUNKS_FOR_QA=2` — 조문 단위 청크(1개)가 필터 미통과
- **수정 방법**: `backend/db/hierarchy_repo.py` line 15 `MIN_CHUNKS_FOR_QA = 1`로 변경
- **우선순위**: 즉시 수정 (기능 블로킹 버그)

---

## 버그 수정 내역

### BUG-1 — 카테고리 필터 (법률 문서 h3 전부 제외) ✅ FIXED
- **파일**: `backend/db/hierarchy_repo.py`
- **변경**: `MIN_CHUNKS_FOR_QA = 2` → `MIN_CHUNKS_FOR_QA = 1`
- **이유**: 법령 조문 단위 청크는 1개/조문 구조 → 2 이상 조건 하에 h3 21개 전부 필터 아웃 → h1 리스트 비워져 "전체"만 표시
- **결과**: 카테고리 목록 정상 표시 확인

### BUG-2 — claude-sonnet QA 0건 생성 ✅ FIXED
- **파일**: `backend/generators/qa_generator.py`
- **원인**: `max_tokens=2048` 초과로 JSON 중간 절단 (`"inte` 앞에서 잘림) → `json.loads` 실패 → qa_list 빈 배열
  - 한국어 법령 답변은 길어 2048 토큰 부족
  - Gemini는 짧은 답변으로 통과, Claude만 실패
- **수정 1**: `max_tokens=2048 → 8192`
- **수정 2**: `_extract_qa_list(raw)` 헬퍼 추가 — 3단계 파싱 (```json 블록 → 직접 파싱 → regex 패턴) + 실패 시 warning 로그
- **수정 3**: `stop_reason == "max_tokens"` 감지 시 warning 로그 추가
- **결과**: claude-sonnet 정상 생성 확인 (results9: 29 QA)

---

## results9 분석 (개인정보 보호법 — claude-sonnet, 5청크 h1 범위)

> 파일: `docs/eval_sample/results9.json` | 모델: claude-sonnet | 범위: h1='개인정보 보호법 총칙' | 청크 수: 5개

### 핵심 지표

| 지표 | 값 |
|------|-----|
| 총 QA 수 | 29개 |
| Quality pass_rate | **100%** (29/29) |
| avg_quality | 1.00 |
| near_duplicate_rate | **17.24%** (5건 중복) |
| duplication_score | 1.38 |

### 평가

- BUG-2 수정 후 claude-sonnet 정상 작동 확인, quality 100% 달성
- **near_duplicate_rate 17.24% 이슈**: 5청크가 같은 h1 아래 내용이 겹치는데, 청크별 **독립 병렬 생성** 구조라 다른 청크에서 이미 생성된 질문을 모름 → 유사 질문 반복 발생
  - 5청크 × 청크당 최대 8개(기존) = 최대 40개 생성 → 29개 확정 → 중복 5건
- **원인 구조**: 좁은 범위(h1 하나) 선택 + 청크 간 내용 유사성 + 상한 무제한

### 후속 조치 — 중복 방지 이중 대책 적용

#### 대책 1. 청크당 생성 상한 동적 축소 (`prompts.py`)
`build_user_template()` 시그니처에 `total_chunks` 파라미터 추가:

| 선택된 청크 수 | 청크당 최대 | 이전 |
|:-----------:|:---------:|:----:|
| ≤ 3개 | 3개 | 8개 |
| 4~7개 | 4개 | 8개 |
| 8개+ | 6개 | 8개 |

- `<selection_rule>`의 `2~6개` 하드코딩도 `{min_qa}~{n_qa}개`로 동적화

#### 대책 2. 청크 간 near-duplicate 검증 (`worker.py`)
- `_dedup_across_chunks(results, sim_threshold=0.75)` 헬퍼 추가
- 병렬 생성 완료 후 전체 QA를 순서대로 순회
- 공백 제거 + 소문자 정규화 후 `SequenceMatcher` 유사도 ≥ 0.75 → 중복 판정 제거
- 제거 건수 로그 기록 (`청크 간 중복 QA 제거: N개`)
- `import re`, `from difflib import SequenceMatcher` 추가

#### 기대 효과 (results9 기준)
- 5청크 → 최대 `4개 × 5 = 20개` 생성 (기존 40개)
- 생성 후 dedup 필터 → near_duplicate_rate 대폭 감소 예상
