# AutoEval — 개발 세션 기록 (2026-03-20)

---

## ✅ 완료된 작업

### UI / 프론트엔드

| 항목 | 내용 | 파일 |
| ---- | ---- | ---- |
| QA 생성 3-Step UI | 생성 설정 → QA 생성(진행 + 미리보기) → QA 평가 단계 분리 | `QAGenerationPanel.tsx` |
| QA 미리보기 카드 | 생성 완료 후 3개 샘플 카드 표시 (컨텍스트 + Q/A + 의도 배지) | `QAGenerationPanel.tsx` |
| 의도 유형 한국어 표기 | factual→사실형, definition→정의형, procedural→방법형 등 기존 분류 기준 적용 | `QAGenerationPanel.tsx` |
| 터미널 UI 제거 | Step 2에서 터미널 로그 제거 → 카드 기반 미리보기로 대체 | `QAGenerationPanel.tsx` |
| 버튼 크기 통일 | `btnBase` 상수로 `min-w-[148px] px-5 py-2.5` 통일 | `QAGenerationPanel.tsx` |
| Raw data 주석 처리 | 평가 리포트 JSON 다운로드 버튼 비활성화 | `QAEvaluationDashboard.tsx` |

### 백엔드

| 항목 | 내용 | 파일 |
| ---- | ---- | ---- |
| Render PORT 대응 | `port=8000` 하드코딩 → `int(os.getenv("PORT", 8000))` | `main.py` |
| 라우터 URL 정리 | `/analyze-l2-l3` → `/analyze-h2-h3` (함수명과 일관성) | `ingestion_api.py` |

### 계층 구조 네이밍 변경 (l1/l2/l3 → h1/h2/h3)

| 항목 | 내용 | 파일 |
| ---- | ---- | ---- |
| 백엔드 메타데이터 키 | `hierarchy_l1/l2/l3` → `hierarchy_h1/h2/h3` | `ingestion_api.py`, `generation_api.py`, `domain_profiler.py`, `hierarchy_repo.py`, `doc_chunk_repo.py` |
| API 요청/응답 키 | `l1_candidates`, `selected_l1_list`, `l2_l3_master` → `h1_candidates`, `selected_h1_list`, `h2_h3_master` | `ingestion_api.py` |
| 프론트엔드 상태/변수 | `selectedL1s`, `hierarchyL1List` 등 → `selectedH1s`, `hierarchyH1List` 등 | `DataStandardizationPanel.tsx`, `HierarchyConstructionPanel.tsx`, `QAGenerationPanel.tsx` |
| API 클라이언트 | `l1_list`, `l2_by_l1`, `l3_by_l1_l2` → `h1_list`, `h2_by_h1`, `h3_by_h1_h2` | `api.ts` |
| Pipeline 도식 | S2 단계 레이블, S3 필터 레이블 변경 | `PipelineFlow.tsx` |
| DB 마이그레이션 | 기존 저장 데이터 `hierarchy_l1/l2/l3` → `hierarchy_h1/h2/h3` Supabase SQL로 일괄 변환 | Supabase SQL Editor |

### Docker 구성

| 항목 | 내용 | 파일 |
| ---- | ---- | ---- |
| 백엔드 Dockerfile | Python 3.12-slim + uv + curl + TZ=Asia/Seoul | `backend/Dockerfile` |
| 프론트엔드 Dockerfile | Node 빌드 → nginx:alpine 멀티스테이지 (프로덕션) | `frontend/Dockerfile` |
| 프론트엔드 Dockerfile.dev | Node 20-alpine Vite dev server (HMR 지원, `--host 0.0.0.0`) | `frontend/Dockerfile.dev` |
| nginx.conf | SPA 라우팅 + `/api/` 리버스 프록시, `resolver 127.0.0.11` (Docker DNS) | `frontend/nginx.conf` |
| docker-compose.yml | 프로덕션: server/client 서비스, healthcheck, `restart: unless-stopped` | `docker-compose.yml` |
| docker-compose.dev.yml | 개발 오버라이드: 소스 볼륨 마운트, Vite HMR | `docker-compose.dev.yml` |
| .env.example | 환경변수 템플릿 커밋 | `.env.example` |
| nginx upstream DNS 수정 | `resolver 127.0.0.11` + `$upstream` 변수로 동적 DNS 조회 (host not found 해결) | `frontend/nginx.conf` |

### 문서 정리

| 항목 | 내용 | 파일 |
| ---- | ---- | ---- |
| README 아키텍처 | Docker 파일 각 섹션에 통합, 루트 파일 명시 | `README.md` |
| README API 목록 | h1/h2/h3 반영, `/analyze-h2-h3` URL 업데이트 | `README.md` |
| backend README | API 경로 및 설명 h1/h2/h3로 업데이트 | `backend/README.md` |

---

## 📋 예정 계획

| 우선순위 | 항목 | 내용 |
| -------- | ---- | ---- |
| 상 | 실패 데이터 재처리 | 실패 데이터 처리방안 구성 |
| 상 | 골든셋 구축 | `qa_golden_set` 테이블 + 자동 후보 추출 (`DEV_260318v3.md` 참고) |
| 중 | nginx 50x 에러 페이지 | Docker → VPS 직접 배포 전환 시점에 추가 |

---

## 🐳 Docker 주요 명령어

```bash
# 프로덕션 빌드 및 실행
docker compose build
docker compose up -d

# 개발 모드 (HMR)
docker compose -f docker-compose.yml -f docker-compose.dev.yml up

# 로그 확인
docker compose logs -f

# 중지 (컨테이너 유지)
docker compose stop
docker compose start

# 완전 종료 (컨테이너 삭제)
docker compose down
```

---

## 🚀 배포 구성 (Render + Vercel)

| 플랫폼 | 역할 | 환경변수 |
| ------ | ---- | -------- |
| Render | FastAPI 백엔드 | `CORS_ORIGINS=https://autoeval-v1.vercel.app` |
| Vercel | React 프론트엔드 | `VITE_API_URL=https://autoeval-uccr.onrender.com` |

- Render는 `PORT` 환경변수를 자동 주입 → `main.py`에서 `os.getenv("PORT", 8000)`으로 대응
- 로컬 개발 시 `VITE_API_URL` 미설정 → `http://localhost:8000` fallback 자동 적용
