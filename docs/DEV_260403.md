# Development Log: Prompt Optimization Review (2026-04-03)

본 문서는 `backend/generators/prompts.py`의 QA 생성 프롬프트 품질을 점검하고, 향후 고도화 방향을 정리한 리포트입니다.

## 1. 개요

AutoEval 프로젝트의 핵심인 QA 생성 엔진의 품질을 제어하는 프롬프트 시스템을 검토했습니다. 설계상 강점과 실무 적용 시 발생 가능한 잠재적 리스크를 식별하고 이를 해결하기 위한 개선안을 도출했습니다.

## 2. 주요 설계 강점 (Keep)

- **QA 품질 제어 축 명확화**: Groundedness, Single-scope, Depth, Completeness 등의 지표가 RAG 평가와 잘 연결되어 있음.
- **Intent Taxonomy**: 실제 downstream task와 정렬이 잘 된 6가지 의도 유형 정의.
- **Domain Adaptive Engine**: 문서 도메인별 가중치 및 샘플 제공 방식.
- **Trivial QA 방지**: 단순 날짜, 번호, 명칭 등 학습 가치가 낮은 데이터 생성을 엄격히 제한.

## 3. 핵심 개선 과제 (Improve)

### 3.1 [Priority 1] 사전 검증 및 Context Screening 유연화

- **문제**: 현재 "완성된 서술 문서 필수" 기준이 표/불렛 데이터를 불필요하게 스크리닝할 가능성이 높음.
- **해결**: 문법적 완결성보다 **"정보 간 상관관계(Entity-Value-Condition)"** 유무를 기준으로 스크리닝 완화.
- **실행**: 수정 전, 현재 프롬프트가 표/불렛 데이터를 실제 `{"qa_list": []}`로 반환하는지 테스트 코드로 먼저 확인.

### 3.2 Reasoning 필드: '해석' -> '증거 추적(Evidence Trace)'

- **구조적 차이**:
  - **이전 (Interpretation)**: 질문의 배경이나 주관적 논리를 설명. 할루시네이션 유발 포인트.
  - **이후 (Evidence Trace)**: 질문이 컨텍스트의 **어느 구절(Mapping)**에서 왔고, 이를 **어떻게 변형(Formulation)**했는지 기술.
- **효과**: 생성 모델이 답변(A)을 쓸 때 원문의 팩트에 더 집중하게 되어, 결과적으로 성능 평가 점수(Groundedness)가 향상됨.

### 3.3 Depth vs Single-scope 원칙 충돌 해결

- **문제**: "2개 이상 연결(Depth)" 하려다 "차원을 섞는(Mixed Dimension)" 실수 발생.
- **해결**: Depth의 범위를 **"동일한 질문 차원 내에서의 복수 요소 결합"**으로 한정.

### 3.4 다국어(EN/KO) 정합성 및 Diversity Rules

- **Diversity**: 생성 전 intent 분포 계획 수립 단계를 프롬프트 상단에 배치.
- **Multilingual**: 한국어의 '답변 완전성(Answer Completeness)' 규칙을 영어 프롬프트에도 이식.

## 3.5 핵심 개선 과제 및 충돌 지침 분석

### 1. Context Screening: Ingestion과 Generation의 불일치
*   **현상**: Ingestion 단계에서 PDF/Docx의 표를 **Markdown Table** 형식으로 충실히 추출하고 있음.
*   **충돌**: 현재 V1 프롬프트의 "완성된 서술 문장(주어+서술어) 필수" 지침이 Markdown 형식을 '단순 항목 나열'로 오인하여 우수한 정보원을 차단함.
*   **해결**: "문장 형태" 대신 "의미 있는 정보 관계(Entity-Value-Condition)"가 성립하면 수용하도록 지침 완화.

### 2. Depth vs Single-scope: 차원 혼합 방지
*   **현상**: "2개 이상의 요소 연결(Depth)" 지침이 "질문 차원 하나만 다룰 것(Single-scope)" 지침과 충돌하여 모델이 질문을 복잡하게 꼬는 경향 발생.
*   **해결**: Depth를 "동일 질문 차원 내의 복수 항목/조건 결합"으로 재정의하여 의미 있는 정보 밀도는 높이되 차원 혼합은 금지.

### 3. Reasoning: 해석에서 증거 매핑으로
*   **현상**: 기존의 "해석" 요구 사항은 모델의 주관적 개입을 유도하여 할루시네이션(Grounding 실패)의 원인이 됨.
*   **해결**: Reasoning을 **"컨텍스트 문장/항목 매핑 및 구성 방식 서술(Evidence Trace)"**로 변경하여 근거성을 강제함.

## 4. 개선 전/후 예상 비교

| 항목            | 현재 (V1)                              | 수정 후 (개선안)                             |
| :-------------- | :------------------------------------- | :------------------------------------------- |
| **답변 스타일** | 요약 및 해석 포함 (설명조)             | **팩트 및 수치 중심 (객관조)**               |
| **어휘 선택**   | '대폭', '가속화' 등 주관적 형용사 포함 | **컨텍스트의 수치를 직접 인용 및 보존**      |
| **Reasoning**   | "왜" 만들었는지 논리 설명              | **"어디에서" 가져왔는지 증거(Mapping) 제시** |
| **평가 기대치** | 근거성(Groundedness) 감점 위험         | **근거성 및 정합성 1.0 유지에 최적화**       |

## 5. 향후 고도화 제안 (보류)

> [!NOTE]
> 아래 항목은 아이디어 차원에서 정리해두며, 이번 개선 작업 범위에서는 제외합니다.

- **Difficulty 레벨 추가**: 사후 평가를 위한 등급 도입.
- **Answer Span Anchoring**: 원문 보존 가이드 극대화.

---

## 6. 테스트 결과 및 보완 사항 (2026-04-03 실습 기반)

### 6.1 테스트 케이스 분석 — Round 1 (AI/PIA 평가주체 컨텍스트)

| 케이스 | 내용 | 결과 | 분석 및 보완 |
| :--- | :--- | :--- | :--- |
| **케이스 1** | AI vs PIA 평가주체 비교 (표) | **Groundedness 0.00** | **[실패]** "민간 vs 공공" 성격 차이는 명시되었으나, 특정 제도에 특정 주체를 명시적으로 할당하는 문장이 없어 LLM이 논리적 추론(Inference)을 수행함. 평가 모델은 이를 할루시네이션으로 판단. |
| **케이스 2** | 조건부 중복 해소 방안 (표) | **Pass (1.00)** | **[성공]** "민간 확대 시~ 인정·보완 방식으로 해소"라는 특정 문구의 팩트를 정확히 추출함. |
| **케이스 3** | 디지털의료제품 상세 항목 (주석) | **Pass (1.00)** | **[성공]** 하단 별표(※) 주석에 나열된 다수의 평가 요소를 누락 없이 정확히 리스트업함. |

### 6.2 테스트 케이스 분석 — Round 2 (승용차 수출입 통계 컨텍스트)

동일 컨텍스트(1분기 승용차 교역 현황 보도자료 요약)에서 4개 QA를 생성하여 평가.

| 샘플 | 질문 유형 | avg 품질 | 실패 위치 | 핵심 원인 |
| :--- | :--- | :--- | :--- | :--- |
| **샘플 1** | 역사적 기록 + 증가세 유지 조건 | **0.50** | 질문 생성 | Depth 채우려다 컨텍스트에 없는 "조건" 개념 추가 |
| **샘플 2** | 수출·수입 증감률 + 교역 금액 차이 | **0.70** | 답변 생성 | "교역 금액 차이(131억)"를 계산해서 제시하지 않음 |
| **샘플 3** | 수출·수입 추세 지속성 비교 | **1.00** | — | 팩트 직접 매핑, 추론 없음 |
| **샘플 4** | 수출액 규모 + 수입액 감소 폭 | **1.00** | — | 팩트 직접 매핑, 추론 없음 |

#### 샘플별 상세 분석

**샘플 1 — 질문 생성 단계 실패**
- 질문이 "증가세의 유지 조건"을 묻고 있으나, 컨텍스트에는 "8분기 연속 증가세"라는 **결과**만 있고 **조건**은 명시되지 않음.
- Depth 원칙("동일 차원 내 2가지 요소 연결")을 맞추려다 컨텍스트 밖 개념을 질문에 삽입한 케이스.
- Reasoning 1단계("근거 문장 매핑")에서 "조건"에 해당하는 문장을 찾을 수 없었어야 하나, 차단되지 않음.

**샘플 2 — 답변 생성 단계 실패**
- 질문 자체는 유효 — "158억", "27억" 두 수치가 모두 컨텍스트에 존재함.
- 답변이 "교역 금액의 차이(158 − 27 = 131억 달러)"를 계산하여 제시하지 않아 완전성 0.70.
- 현재 완전성 규칙이 항목 열거에만 집중되어 있고, **수치 간 파생 계산형 정보**를 포괄하지 못함.

**샘플 3, 4 — 이상적 케이스**
- 컨텍스트에 직접 명시된 두 팩트를 추론 없이 비교·결합.
- "8분기 연속 증가 vs 3분기 연속 감소" / "158억 + 29.1% 감소" — 모두 직접 추출 가능.

### 6.3 향후 개선 포인트

#### [프롬프트 레벨] 생성 품질 강화 — **적용 완료 (2026-04-03)**

1. **`plan_before_generate` 4단계 게이트로 재구성** ✅
   - 기존: "intent 조합 계획 수립" 한 줄 지침
   - 변경: 질문 생성 전 4단계 요소별 검증 게이트로 구체화
     1. 컨텍스트에서 직접 추출 가능한 정보 요소 목록화
     2. 각 질문 후보의 구성 요소별 컨텍스트 문장 매핑 가능 여부 확인
     3. 매핑 불가 요소 포함 질문은 해당 요소 제거 또는 질문 전체 폐기
     4. 모든 요소가 근거 있는 질문만 최종 확정
   - 적용 위치: `USER_TEMPLATE_KO_V1` + `build_user_template()` 양쪽

2. **계산형 파생 정보 완전성 규칙 추가** ✅
   - 기존: `[답변 완전성]`이 항목 열거("N개 중 N개 모두")에만 특화, 수치 파생 계산 미포괄
   - 변경: `[계산형 파생 정보]` 섹션 신규 추가
     - 두 수치가 컨텍스트에 모두 있고 질문이 차이·합산·비율 요구 시 계산 결과 명시 의무화
     - 수치 하나라도 없으면 계산 금지 (할루시네이션 방지 조건 명시)
   - 적용 위치: `SYSTEM_PROMPT_KO_V1` + `SYSTEM_PROMPT_EN_V1` 양쪽

3. **추론 경계 금지 패턴 구체화** ✅
   - 기존: `groundedness_check`의 "유추 금지" 한 줄 — 추상적이어서 모델이 잘 지키지 않음
   - 변경: `[추론 경계 — 금지 패턴]` 섹션 신규 추가, 예시 기반 구체화
     - "A와 B가 각각 언급되었더라도 A→B 관계가 명시되지 않으면 질문 요소 사용 금지"
     - "문맥상 당연한 조건·이유도 직접 서술 근거 없이는 사용 금지"
     - 샘플 1("증가세의 유지 조건") 을 직접 예시로 삽입
   - 적용 위치: `USER_TEMPLATE_KO_V1` `groundedness_check` + `build_user_template()` 동일 섹션

---

#### [파이프라인 레벨] reasoning 필드 평가 연동

**현황 — reasoning은 평가에 전혀 반영되지 않는 dead field**

코드 흐름 추적 결과:
```
생성 단계: LLM이 reasoning 포함 JSON 생성 → Supabase 저장 (reasoning 보존)
    ↓
평가 로딩 (pipeline.py:513-520): q, a, context, qa_id, intent, docId만 추출
    → reasoning 여기서 드롭
    ↓
_rag_worker: q, a, context만 사용
_quality_worker: q, a, context, intent만 사용
```

**평가에 reasoning을 연결할 경우의 가치**

| 평가 지표 | 현재 방식 | reasoning 연결 시 |
| :--- | :--- | :--- |
| **Groundedness** | answer vs context 직접 비교 | reasoning의 "근거 문장 매핑"을 anchor로 — 모델이 인용한 문장이 실제 context에 존재하는지 검증 가능 |
| **Context Relevance** | question vs context 비교 | reasoning이 직접 인용인지 논리 추론인지 구분 → 추론형 QA에 더 엄격한 기준 적용 가능 |
| **Completeness** | answer 커버리지 평가 | reasoning의 "질문 구성 방식"으로 의도한 범위 파악 → 부분 답변 탐지 정밀도 향상 |

**개선 옵션**

- **옵션 A (경량, 우선 적용)**: `pipeline.py` 로딩 단계에서 reasoning을 함께 전달하고, `rag_triad.py` groundedness 프롬프트에 힌트로 추가. 생성 모델이 어떤 문장을 근거로 답변을 구성했는지 평가 모델이 교차 검증하도록 유도.
- **옵션 B (고급, 추후)**: reasoning의 매핑 문장이 context에 실제로 존재하는지 검증하는 별도 Layer 추가. 매핑 실패 → 잠재적 할루시네이션 플래그 처리.

**수정 대상 파일**
- `backend/evaluators/pipeline.py` — QA 로딩 시 `reasoning` 필드 보존
- `backend/evaluators/rag_triad.py` — groundedness 평가 프롬프트에 reasoning 블록 추가

---

### 6.4 테스트 케이스 분석 — Round 3 (승용차 수출입 단가/비중 컨텍스트)

동일 컨텍스트(수출입 단가 및 차종별 비중 표)에서 4개 QA 생성·평가.

| 샘플 | 질문 유형 | avg 품질 | 결과 | 핵심 원인 |
| :--- | :--- | :--- | :--- | :--- |
| **샘플 1** | 배기량별 미국 수출 비중 열거 (list) | **1.00** | 성공 | 수치 4개 완전 열거 |
| **샘플 2** | 차종별 강세 수출 대상국 비교 (comparison) | **1.00** | 성공 | 직접 팩트 매핑 |
| **샘플 3** | 독일·미국 수입 비중 직접 추출 (fact) | **1.00** | 성공 | 두 수치 직접 추출 |
| **샘플 4** | 수출·수입 최대 교역국 + 주요 차종 (comparison) | **0.50** | 실패 | 수입 차종 세부 항목 누락 |

**라운드별 성과 추이**

| 라운드 | 성공/전체 | 주요 실패 패턴 |
| :--- | :--- | :--- |
| Round 1 | 2/3 | 논리 추론으로 할루시네이션 |
| Round 2 | 2/4 | 질문 생성 이탈, 계산형 누락 |
| **Round 3** | **3/4** | 답변 완전성 — 요약 문장에서 조기 종결 |

Round 2 실패 유형(표 기반 수치 추출, 비교형)이 Round 3에서 모두 통과 → 이전 프롬프트 수정 효과 확인.

#### 샘플 4 상세 분석

컨텍스트에는 수입 차종 정보가 두 층위로 존재:
- **요약 문장**: "수입은 독일 차량을 선호"
- **세부 문장**: "중형·대형·친환경 차량은 독일 선호, 소형 차량은 네덜란드·프랑스 선호"

모델이 요약 문장("독일 차량 선호")에서 답변을 종결하고 뒤에 있는 세부 문장을 무시함. 기존 `[답변 완전성]` 규칙은 "N개 항목 열거" 패턴에 특화되어 있어 이 케이스를 커버하지 못함.

**신규 보완 패턴**: 컨텍스트에 요약 문장과 세부 문장이 공존할 때, 질문이 세부 항목을 요구하면 요약에서 답변을 종결하지 말고 세부 문장까지 포함해야 함.
- 적용 위치: `SYSTEM_PROMPT_KO_V1` + `SYSTEM_PROMPT_EN_V1` `[답변 완전성]` 섹션 — **적용 완료 (2026-04-03)**

---

### 6.5 테스트 케이스 분석 — Round 4 (친환경차/배기량별 수출액 + 교역 현황 컨텍스트)

5개 QA 생성·평가. 2개 컨텍스트 혼합.

| 샘플 | 질문 유형 | avg 품질 | 결과 | 핵심 원인 |
| :--- | :--- | :--- | :--- | :--- |
| **샘플 1** | 친환경차 배기량별 소형/중대형 수출액 + 격차 | **0.69** | 실패 | 독립 분류 교차 결합 + 생성-평가 기준 불일치 |
| **샘플 2** | 하이브리드차 수출 실적 특징 | **0.88** | 실패 | 정성 설명에 수치 근거 누락 |
| **샘플 3** | 수출 8분기 연속 증가 추세 | **1.00** | 성공 | 직접 팩트 추출 |
| **샘플 4** | 수출·수입 증감 방향 비교 | **1.00** | 성공 | 직접 팩트 비교 |
| **샘플 5** | 금액·대수 기준 증감 방향 (복합) | **1.00** | 성공 | 두 축 직접 추출 |

#### 신규 발견 패턴

**패턴 A — 분류 체계 교차 결합 (샘플 1)**

컨텍스트의 표 구조:
```
[친환경차 및 배기량별 수출액]
  ├── 친환경차 종류별: 플러그인하이브리드 / 하이브리드 / 전기차
  └── 배기량별:       경차 / 소형차 / 중대형차  ← 별도 독립 분류
```

질문이 "친환경차 배기량별" 수출액을 묻고 있으나, 두 분류는 컨텍스트에서 **교차되지 않는 독립 축**. "친환경차 × 배기량별"의 교집합은 컨텍스트에 존재하지 않음. 포인트 3(추론 경계)에서 잡아야 했지만 놓친 케이스 — 두 분류가 각각 컨텍스트에 있더라도 교차 결합 개념은 없을 수 있음.

**패턴 B — 정성 표현에 수치 근거 누락 (샘플 2)**

답변이 "역대 분기 최대 수출실적을 기록하며 호조를 보였습니다"(정성 설명)만 포함하고, 컨텍스트에 있는 분기별 수치(하이브리드 '24.1Q 20.9억$, 역대 최고)를 누락. "역대 최대", "급증", "강세", "호조" 등 정성적 표현을 사용할 때 이를 뒷받침하는 수치 근거를 함께 제시하지 않은 케이스.

#### 부수 발견 — 생성-평가 기준 불일치 — **적용 완료 (2026-04-03)**

샘플 1에서 격차 계산(71 − 58 = 13억)은 `[계산형 파생 정보]` 지침에 따라 올바르게 생성했지만, 평가 모델(rag_triad.py groundedness)이 근거성 0.70을 부여. 생성 프롬프트는 계산을 요구하는데 평가 모델은 계산 결과를 할루시네이션으로 판단하는 구조적 불일치.
- **적용 내용**: `rag_triad.py` groundedness 평가 프롬프트 수정
  - `<constraints>`에 "두 원본 수치가 모두 컨텍스트에 있는 계산 결과는 Strong 근거로 인정" 조건 추가
  - Step 2에 "계산형 수치는 원본 두 값이 컨텍스트에 있는지 확인" 단계 명시
  - Step 3 Strong 기준에 "컨텍스트 수치 기반 계산 결과" 명시적 포함

#### 프롬프트 보완 사항 — **적용 완료 (2026-04-03)**

1. **분류 교차 결합 금지** ✅ — `[추론 경계]` 금지 패턴에 추가
   - 적용 위치: `USER_TEMPLATE_KO_V1` (예시 포함 상세 버전) + `build_user_template()` (간결 버전)
   - 내용: A 분류와 B 분류가 각각 컨텍스트에 존재해도 교차 결합(A × B)이 명시되지 않으면 질문화 금지

2. **정성 표현 수치 근거 동반 의무** ✅ — `[답변 완전성]` 섹션에 추가
   - 적용 위치: `SYSTEM_PROMPT_KO_V1` + `SYSTEM_PROMPT_EN_V1` 양쪽
   - 내용: "역대 최대", "급증", "강세", "호조" 등 정성 표현 사용 시 컨텍스트의 수치 근거 동반 의무화

---

**수정일자**: 2026-04-03
