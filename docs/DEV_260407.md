# 프론트엔드 리팩토링 계획 (2026-04-07)

---

## 실측 현황 요약

| 파일 | 라인 수 | 주요 문제 |
|------|---------|-----------|
| `src/lib/exportUtils.ts` | 866 | 중복 상수, 인라인 JS·SVG 대규모 문자열, toKST 중복 |
| `src/components/generation/QAGenerationPanel.tsx` | 841 | 목록 **미포함** 대형 파일, 직접 fetch() 4곳 |
| `src/components/evaluation/QAEvaluationDashboard.tsx` | 764 | useState 12개, 데이터 페칭·렌더 혼재 |
| `src/components/standardization/DataStandardizationPanel.tsx` | 604 | ~~로컬 fetchWithRetry 중복 정의~~ ✅ 이관 완료 |
| `src/lib/api.ts` | 150 | ~~try/catch 7회 반복~~ ✅ 공통 래퍼 추출 완료, 에러 메시지 개선 완료 |
| `src/App.tsx` | 142 | prop drilling 경미 (현수준 관리 가능) |

---

## 1. 즉시 처리 (위험도 낮음, 효과 명확)

### 1-A. `package.json` 불필요 의존성 제거
- **삭제 대상**: `better-sqlite3`, `express`, `dotenv`
  - `src/` 어디에서도 import 없음, 프론트엔드와 무관
- **devDependencies로 이동**: `tsx`
- **확인 필요**: `@google/genai` — `main.tsx`에서 grep 히트됐으나 실제 사용처 불명확, 재검토 필요

### 1-B. `api.ts` — 공통 래퍼 추출 ✅ 완료
- ~~현황: `fetch + try/catch` 블록이 7개 함수에 동일 패턴으로 반복~~
- ~~목표: `apiFetch<T>(url, options?)` 래퍼 하나로 통합~~
- ~~**추가 발견**: `DataStandardizationPanel.tsx:7`에 `fetchWithRetry` 로컬 정의가 중복 존재 → `api.ts`로 이관 후 공통화~~

---

## 2. 중기 작업 (코드 품질·유지보수)

### 2-A. `exportUtils.ts` (866줄) 분리 ✅ 완료 (260407)
- ~~중복 상수 제거 및 4개 파일로 분리~~

### 2-B. `QAEvaluationDashboard.tsx` (764줄) — 훅 추출
- useState 12개가 단일 컴포넌트에 집중
- **추출 대상**:
  - `useEvaluationData(evalJobId)` — report fetch, loading/error 상태
  - `useEvalHistory(initialEvalDbId)` — historyList, selectHistory, historyQaPreview 로직
- 훅 추출만으로 컴포넌트 본체 ~300줄 이하로 축소 가능

### 2-C. `QAGenerationPanel.tsx` (841줄) — ✅ fetch() 직접 호출 제거 완료 (260407)
- ~~문서에 없었으나 실측 841줄로 `exportUtils.ts` 다음 두 번째 대형 파일~~
- ~~**직접 fetch() 4곳** (api.ts 우회): lines 263, 278, 312, 345~~
- `QAGenerationPanel.improved.tsx`가 별도 존재 — 역할·사용 여부 확인 필요

### 2-D. `DataStandardizationPanel.tsx` (604줄) — 기능 단위 분리
- `fetchWithRetry` → api.ts 공통화 (1-B와 연동)
- 파일 업로드, 분석 결과 뷰, 계층 구조 트리 뷰를 각각 독립 컴포넌트로 분리

---

## 3. 공통 UI 컴포넌트 추출

- 현황: 버튼, 카드, 인라인 스타일링 반복 패턴이 코드 곳곳에 산재
- 목표: `src/components/ui/` 에 `Button`, `Card`, `StatusBadge` 등 공통화
- 복잡한 조건부 클래스는 `cva` (Class Variance Authority) 활용 가능

---

## 4. 장기/선택적

### 4-A. 라우팅 도입
- 현황: `App.tsx`의 `activeTab` 상태로 수동 전환 (142줄, 현수준 관리 가능)
- 딥링크·히스토리 back/forward 필요 시점에 `react-router-dom` 또는 `TanStack Router` 도입
- **현재는 우선순위 낮음**

### 4-B. 상태 관리 (Zustand)
- 현황: prop drilling이 `activeTab`, `currentFilename`, `taggingVersion`, `notifications` 정도
- 아직 심각한 수준 아님, 컴포넌트 수 증가 시 재검토

### 4-C. 런타임 타입 검증 (Zod)
- API 응답의 `any` 캐스팅이 여러 곳에 존재 (예: `QAEvaluationDashboard.tsx:76`)
- Zod 스키마로 프론트-백엔드 타입 불일치 조기 감지 가능

---

## 작업 우선순위

```
1순위 (즉시): package.json 정리 → api.ts 공통 래퍼 + fetchWithRetry 이관  ✅ 완료 (260407)
2순위 (중기): QAGenerationPanel fetch() 직접호출 → api.ts 위임           ✅ 완료 (260407)
              exportUtils.ts 분리 (중복 상수 제거 포함)                   ✅ 완료 (260407)
3순위 (여유): QAEvaluationDashboard 훅 추출
              DataStandardizationPanel 컴포넌트 분리
4순위 (선택): 라우팅, Zustand, Zod
```

---

## 작업 로그

### 260407 — 1순위 완료

#### 1-A. `package.json` 불필요 의존성 제거
- `dependencies`에서 삭제: `better-sqlite3`, `express`, `dotenv`, `@google/genai`
  - `src/` 전체 grep으로 사용처 없음 확인
- `devDependencies`에서 삭제: `@types/express`
- `tsx`: `dependencies` → `devDependencies` 이동

#### 1-B. `api.ts` 공통 래퍼 추출
- `ApiResponse` 인터페이스 `export` 공개
- `apiFetch<T>(url, options?)` 내부 헬퍼 추가 — 7개 함수의 `try/catch` 반복 제거
- `apiFetchWithRetry<T>(url, options, retries?, delayMs?)` 공개 export 추가
  - 에러 응답 바디의 `detail` 필드까지 파싱해 `error`에 전달 (503 overload 판별 유지)

#### 1-B 연동. `DataStandardizationPanel.tsx` fetchWithRetry 이관
- 로컬 `fetchWithRetry` 함수 정의 삭제
- `apiFetchWithRetry` import 후 대체 — 동작·에러 처리 동일 유지
- `tsc --noEmit` exit code 0 확인

#### 1-C. `api.ts` 에러 메시지 전면 개선 ✅ 완료 (260407)

**`httpStatusToMessage` 헬퍼 추가** (`apiFetch` 내부 사용)

| HTTP 상태 | 기존 | 수정 후 |
|-----------|------|---------|
| 400 | `HTTP 400` | `요청 형식이 올바르지 않습니다. 입력값을 확인해 주세요.` |
| 401 / 403 | `HTTP 401` / `HTTP 403` | `접근 권한이 없습니다.` |
| 404 | `HTTP 404` | `요청한 데이터를 찾을 수 없습니다.` |
| 429 | `HTTP 429` | `요청이 너무 많습니다. 잠시 후 다시 시도해 주세요.` |
| 5xx | `HTTP 500` 등 | `서버에 일시적인 문제가 발생했습니다. 잠시 후 다시 시도해 주세요.` |

**`apiFetchWithRetry` 최종 실패 메시지**
- 기존: `'fetch failed'` (영문, 맥락 없음)
- 수정: `'서버에 연결할 수 없습니다. 네트워크 상태를 확인하거나 잠시 후 다시 시도해 주세요.'`

**`mapErrorToMessage` 케이스 추가 및 워딩 개선**

| 케이스 | 기존 | 수정 후 |
|--------|------|---------|
| ① `계층` + `청크가 없습니다` | `선택한 계층(H1/H2)에 해당하는 데이터가 없습니다.\nHierarchy 태깅(Pass3)이 완료됐는지 확인해 주세요.` | `선택한 카테고리에 해당하는 문서 데이터가 없습니다.\n'Documents' 페이지에서 컨텍스트 분석이 완료됐는지 확인해 주세요.` |
| ② `태깅이 완료되지 않았습니다` | `Hierarchy 태깅(Pass3)이 완료되지 않았습니다.\nPass3을 먼저 실행해 주세요.` | `카테고리 분류가 완료되지 않았습니다 (미처리 N/M개).\n'Documents' 페이지에서 컨텍스트 분석을 다시 실행해 주세요.` |
| ③ `QA 생성에 필요한 문서 청크를 찾을 수 없습니다` | **미처리** (백엔드 원문 노출) | `업로드된 문서가 없습니다.\n'Documents' 페이지에서 문서를 업로드하고 카테고리 분류를 완료해 주세요.` |
| ④ `Failed to fetch` | **미처리** | `네트워크 연결을 확인해 주세요.` |
| ⑤ `HTTP 4xx` / `HTTP 5xx` | **미처리** (코드 문자열 노출) | `일시적인 오류가 발생했습니다. 잠시 후 다시 시도해 주세요.` |
| ⑥ fallback | `` `오류가 발생했습니다: ${error}` `` (원문 노출) | `일시적인 오류가 발생했습니다. 문제가 지속되면 관리자에게 문의해 주세요.` |

- `tsc --noEmit` exit code 0 확인

---

## 미완료 작업 메모

### `api.ts` 에러 메시지 개선 ✅ 완료 (260407)

→ 작업 로그 `1-C` 항목 참조

---

#### 2-A. `exportUtils.ts` (866줄) → `exportUtils/` 폴더 분리

**파일 구조**
```
src/lib/exportUtils/
  types.ts       (34줄)  — EvaluationData 인터페이스
  xlsxBuilder.ts (114줄) — buildWorkbook, exportToCSV
  htmlBuilder.ts (615줄) — SVG 3종 + buildHTMLContent + exportToHTML
  index.ts       (83줄)  — exportToJSON, exportToZip + re-exports (facade)
```

**중복 제거**
- `toKST()` 삭제 → `formatKST()`(`evalChartUtils.ts`) 로 통합
- `INTENT_KR` 삭제 → `types/evaluation.ts` import
- `FAILURE_KR` → `xlsxBuilder.ts` 로컬 유지 (xlsx에서만 사용)

**기존 import 경로 유지**: `from '@/src/lib/exportUtils'` → index.ts 자동 해석으로 변경 없음
- `tsc --noEmit` exit code 0 확인

---

#### 2-C. `QAGenerationPanel.tsx` fetch() 직접 호출 제거

**`api.ts` 신규 함수 추가**
- `getGenStatus(jobId)` — `/api/generate/{jobId}/status`
- `getGenPreview(jobId, limit?)` — `/api/generate/{jobId}/preview?limit=...`

**`QAGenerationPanel.tsx` 변경**
- import에서 `API_BASE` 제거, `getGenStatus`, `getGenPreview`, `getEvalStatus`, `evaluateQA` 추가
- fetch #1 (생성 폴링 상태) → `getGenStatus(jobId)`
- fetch #2 (QA Preview 조회) → `getGenPreview(jobId, 3)`
- fetch #3 (평가 폴링 상태) → `getEvalStatus(evalJobId)` (기존 함수 재활용)
- fetch #4 (평가 시작 POST) → `evaluateQA({...})` (기존 함수 재활용)
- `tsc --noEmit` exit code 0 확인
