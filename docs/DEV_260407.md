# 프론트엔드 리팩토링 계획 (2026-04-07)

---

## 실측 현황 요약

| 파일 | 라인 수 | 주요 문제 |
|------|---------|-----------|
| `src/lib/exportUtils/` | 분리 완료 | ✅ types/xlsxBuilder/htmlBuilder/index 4분리 |
| `src/components/generation/QAGenerationPanel.tsx` | 841 | ✅ fetch() 직접 호출 제거 완료 |
| `src/components/generation/QAGenerationPanel.improved.tsx` | 396 | 미사용 파일, 삭제 권장 |
| `src/components/evaluation/QAEvaluationDashboard.tsx` | 764 | useState **16개** (문서 오기재 12개→실측 16개), `as any` 5곳 |
| `src/components/standardization/DataStandardizationPanel.tsx` | 589 | fetch() 직접 호출 2곳 잔존 |
| `src/lib/api.ts` | 182 | ✅ 공통 래퍼 완료 |
| `src/App.tsx` | 142 | taggingTreeData 인라인 타입 반복 |
| `package.json` | — | vite 중복 등록 (dependencies + devDependencies) |

---

## 작업 우선순위

```
즉시:    package.json vite 중복 제거                                       ✅ 완료 (260407)
         QAGenerationPanel.improved.tsx 삭제                               ✅ 완료 (260407)

3순위:   QAEvaluationDashboard 훅 추출 (useEvaluationData/useEvalHistory/useQATable) ✅ 완료 (260407)
         DataStandardizationPanel fetch() 2곳 이관                        ✅ 완료 (260407)
         DataStandardizationPanel 컴포넌트 분리 (업로드/분석/트리 뷰)

겸사겸사: getGenerationModel() 중복 로직 제거
          historyReport / taggingTreeData 인라인 타입 → types/ 분리

4순위:   라우팅, Zustand, Zod
```

---

## 즉시 처리

### package.json — vite 중복 등록 ✅ 완료 (260407)
- `dependencies`에서 `vite` 제거 → `devDependencies`에만 유지

### `QAGenerationPanel.improved.tsx` — 삭제 ✅ 완료 (260407)
- 미사용 파일 삭제 (App.tsx에서 미참조, fetch() 직접 호출 잔존, 중복 import 등)

---

## 3순위 (기존 유지 + 업데이트)

### `QAEvaluationDashboard.tsx` (764줄 → 675줄) — 훅 추출 ✅ 완료 (260407)

**추출된 훅 3개** (`src/components/evaluation/hooks/`)

| 훅 | 관리 상태 | 파일 |
|----|----------|------|
| `useEvaluationData(evalJobId, onNewJob?)` | `loading`, `error`, `report` | `useEvaluationData.ts` |
| `useEvalHistory(initialEvalDbId, onSelect?)` | `historyList`, `historyReport`, `historyQaPreview`, `historyQaLoading`, `selectedHistoryId`, `showHistoryMenu` | `useEvalHistory.ts` |
| `useQATable(qaPreview)` | `qaPage`, `statusFilter`, `sortCol`, `sortDir`, `selectedQA` + 파생 `filteredQA/sortedQA/totalPages/pagedQA` | `useQATable.ts` |

- 컴포넌트 내 `useState`: 16개 → **2개** (`showExportMenu`, `exportLoading`)
- `HistoryReport` 인라인 타입 → `useEvalHistory.ts`에서 `export type HistoryReport` 로 명명 분리
- `onNewJob` / `onSelect` callback ref 패턴으로 훅 간 상태 조율 (clearHistory + resetTable)
- `tsc --noEmit` exit code 0 확인

**잔존 항목 (겸사겸사)**
- `getGenerationModel()` 중복 로직: 인라인(export 데이터 빌드) ↔ 함수 선언 2곳
- `as any` 캐스팅 8곳 — api.ts 구체 제네릭 지정 시 일부 제거 가능 (Zod 도입 전 개선 가능)

### `DataStandardizationPanel.tsx` — fetch() 2곳 이관 ✅ 완료 (260407)

**`api.ts` 신규 함수 추가**
- `uploadDocument(formData)` — `POST /api/ingestion/upload`
- `applyGranularTagging(payload)` — `POST /api/ingestion/apply-granular-tagging`

**컴포넌트 분리** (미완료 — 3순위 유지)
- 파일 업로드 뷰, 분석 결과 뷰, 계층 구조 트리 뷰 → 각각 독립 컴포넌트

### `App.tsx` — taggingTreeData 인라인 타입 반복 (신규 발견)

```ts
// line 18 — App.tsx
useState<{ h1_list: string[]; h2_by_h1: Record<string, string[]>; h3_by_h1_h2: Record<string, string[]> } | null>
```

동일 타입이 `DataStandardizationPanel.tsx`(line 25), `QAGenerationPanel.tsx` props에도 반복.
`types/` 에 `HierarchyTree` 등 공통 타입으로 정의하면 3곳 일괄 교체 가능.

---

## 4순위 (장기/선택적)

### 라우팅 도입
- `App.tsx`의 `activeTab` 상태로 수동 전환 (142줄, 현수준 관리 가능)
- 딥링크 필요 시점에 `react-router-dom` 또는 `TanStack Router` 도입

### 상태 관리 (Zustand)
- prop drilling이 `activeTab`, `currentFilename`, `taggingVersion`, `notifications` 정도
- 컴포넌트 수 증가 시 재검토

### 런타임 타입 검증 (Zod)
- `as any` 캐스팅 근본 해결책
- api.ts 구체 제네릭 타입 지정과 병행

---

## 작업 로그

### 260407 — 1순위 완료

#### 1-A. `package.json` 불필요 의존성 제거
- `dependencies`에서 삭제: `better-sqlite3`, `express`, `dotenv`, `@google/genai`
- `devDependencies`에서 삭제: `@types/express`
- `tsx`: `dependencies` → `devDependencies` 이동

#### 1-B. `api.ts` 공통 래퍼 추출
- `ApiResponse` 인터페이스 `export` 공개
- `apiFetch<T>(url, options?)` 내부 헬퍼 추가 — 7개 함수의 `try/catch` 반복 제거
- `apiFetchWithRetry<T>(url, options, retries?, delayMs?)` 공개 export 추가
  - 에러 응답 바디의 `detail` 필드까지 파싱해 `error`에 전달 (503 overload 판별 유지)

#### 1-B 연동. `DataStandardizationPanel.tsx` fetchWithRetry 이관
- 로컬 `fetchWithRetry` 함수 정의 삭제
- `apiFetchWithRetry` import 후 대체 — 동작·에러 처리 동일 유지
- `tsc --noEmit` exit code 0 확인

#### 1-C. `api.ts` 에러 메시지 전면 개선

**`httpStatusToMessage` 헬퍼 추가**

| HTTP 상태 | 기존 | 수정 후 |
|-----------|------|---------|
| 400 | `HTTP 400` | `요청 형식이 올바르지 않습니다. 입력값을 확인해 주세요.` |
| 401 / 403 | `HTTP 401/403` | `접근 권한이 없습니다.` |
| 404 | `HTTP 404` | `요청한 데이터를 찾을 수 없습니다.` |
| 429 | `HTTP 429` | `요청이 너무 많습니다. 잠시 후 다시 시도해 주세요.` |
| 5xx | `HTTP 500` 등 | `서버에 일시적인 문제가 발생했습니다. 잠시 후 다시 시도해 주세요.` |

**`mapErrorToMessage` 케이스 추가 및 워딩 개선**

| 케이스 | 수정 후 |
|--------|---------|
| ① `계층` + `청크가 없습니다` | `선택한 카테고리에 해당하는 문서 데이터가 없습니다.` |
| ② `태깅이 완료되지 않았습니다` | `카테고리 분류가 완료되지 않았습니다 (미처리 N/M개).` |
| ③ `QA 생성에 필요한 문서 청크를 찾을 수 없습니다` | `업로드된 문서가 없습니다.` |
| ④ `Failed to fetch` | `네트워크 연결을 확인해 주세요.` |
| ⑤ `HTTP 4xx/5xx` | `일시적인 오류가 발생했습니다. 잠시 후 다시 시도해 주세요.` |
| ⑥ fallback | `일시적인 오류가 발생했습니다. 문제가 지속되면 관리자에게 문의해 주세요.` |

- `tsc --noEmit` exit code 0 확인

---

### 260407 — 2순위 완료

#### 2-A. `exportUtils.ts` (866줄) → `exportUtils/` 폴더 분리

```
src/lib/exportUtils/
  types.ts        — EvaluationData 인터페이스
  xlsxBuilder.ts  — buildWorkbook, exportToCSV
  htmlBuilder.ts  — SVG 3종 + buildHTMLContent + exportToHTML
  index.ts        — exportToJSON, exportToZip + re-exports (facade)
```

- `toKST()` 삭제 → `formatKST()`(`evalChartUtils.ts`) 통합
- `INTENT_KR` 삭제 → `types/evaluation.ts` import
- `FAILURE_KR` → `xlsxBuilder.ts` 로컬 유지 (xlsx 전용)
- 기존 `from '@/src/lib/exportUtils'` import 경로 변경 없음 (index.ts 자동 해석)
- `tsc --noEmit` exit code 0 확인

#### 2-C. `QAGenerationPanel.tsx` fetch() 직접 호출 제거

**`api.ts` 신규 함수 추가**
- `getGenStatus(jobId)` — `/api/generate/{jobId}/status`
- `getGenPreview(jobId, limit?)` — `/api/generate/{jobId}/preview?limit=...`

**`QAGenerationPanel.tsx` 변경**
- import에서 `API_BASE` 제거, `getGenStatus`, `getGenPreview`, `getEvalStatus`, `evaluateQA` 추가
- fetch #1 (생성 폴링) → `getGenStatus(jobId)`
- fetch #2 (QA Preview) → `getGenPreview(jobId, 3)`
- fetch #3 (평가 폴링) → `getEvalStatus(evalJobId)` (기존 재활용)
- fetch #4 (평가 시작) → `evaluateQA({...})` (기존 재활용)
- `tsc --noEmit` exit code 0 확인

---

### 260407 — 즉시·3순위 완료

#### 즉시-1. `package.json` vite 중복 제거
- `dependencies`에서 `vite: ^6.2.0` 삭제 → `devDependencies`에만 유지

#### 즉시-2. `QAGenerationPanel.improved.tsx` 삭제
- App.tsx 미참조, fetch() 직접 호출 잔존, 중복 import 등 — 참조 가치 없어 삭제

#### 3순위-A. `DataStandardizationPanel.tsx` fetch() 2곳 이관

**`api.ts` 신규 함수 추가**
- `uploadDocument(formData: FormData)` — `POST /api/ingestion/upload`
- `applyGranularTagging(payload)` — `POST /api/ingestion/apply-granular-tagging`

**`DataStandardizationPanel.tsx` 변경**
- fetch #1 (파일 업로드) → `uploadDocument(formData)` — `res.ok` 분기 → `res.success` 분기로 통일
- fetch #2 (태깅 적용) → `applyGranularTagging({...})` — `taggingRes.ok` 분기 → `taggingRes.success` 분기로 통일
- `API_BASE` import 유지 (analyze-hierarchy `apiFetchWithRetry` 호출에서 여전히 사용)
- `tsc --noEmit` exit code 0 확인

#### 3순위-B. `QAEvaluationDashboard.tsx` 훅 추출 (764줄 → 675줄)

**신규 파일** (`src/components/evaluation/hooks/`)

| 파일 | 관리 상태 |
|------|----------|
| `useEvaluationData.ts` | `loading`, `error`, `report` + evalJobId 변경 시 fetch |
| `useEvalHistory.ts` | `historyList`, `historyReport`, `historyQaPreview`, `historyQaLoading`, `selectedHistoryId`, `showHistoryMenu` + `selectHistory`, `clearHistory` |
| `useQATable.ts` | `qaPage`, `statusFilter`, `sortCol`, `sortDir`, `selectedQA` + 파생 `filteredQA/sortedQA/totalPages/pagedQA` + `resetTable` |

**훅 간 조율 패턴**
- `onNewJob` / `onSelect` 콜백을 내부 ref로 저장 → 클로저 forward-reference 없이 안전하게 `clearHistory()` + `resetTable()` 호출
- `HistoryReport` 인라인 타입 → `useEvalHistory.ts`에서 `export type HistoryReport`로 명명 분리

**컴포넌트 변화**
- `useState`: 16개 → 2개 (`showExportMenu`, `exportLoading`)
- `useEffect`: 4개 → 0개 (모두 훅 내부로 이동, export 메뉴 외부클릭 effect 포함)
- `tsc --noEmit` exit code 0 확인
