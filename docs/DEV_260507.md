# README 전면 검토 및 수정 — 2026-05-07

전체 코드베이스 대조 기반 README 3종(최상위 / backend / frontend) 검토 결과 및 수정 내역.

---

## 수정 파일 목록

| 파일 | 수정 건수 |
|------|----------|
| `backend/README.md` | 3건 |
| `frontend/README.md` | 6건 |

> 최상위 `README.md`는 코드와 일치 확인 — 수정 없음.

---

## backend/README.md

### 1. 오타 수정

| 위치 | 수정 전 | 수정 후 |
|------|--------|--------|
| Render 슬립 방지 섹션 | `폙 간격 : 5분` | `폴링 간격 : 5분` |

### 2. `MIN_CHUNKS_FOR_QA` 값 오류 (2곳)

**근거**: `backend/db/hierarchy_repo.py:16` → `MIN_CHUNKS_FOR_QA = 1`

| 위치 | 수정 전 | 수정 후 |
|------|--------|--------|
| 인제스션 파이프라인 흐름 (`/hierarchy-list`) | `MIN_CHUNKS_FOR_QA=2 + MIN_CONTENT_CHARS=300` | `MIN_CHUNKS_FOR_QA=1 OR MIN_CONTENT_CHARS=300` |
| 주요 설계 결정 표 (H2/H3 최소 조건) | `MIN_CHUNKS=2`, AND 조건 | `MIN_CHUNKS_FOR_QA=1`, OR 조건 |

> 필터 조건도 AND → OR로 정정. 실제 코드(`hierarchy_repo.py:106,127`)는 h3는 두 조건 AND, h2는 OR 적용.

### 3. QA 상태 판정 표 — 상태값 오류

**근거**: `frontend/src/types/evaluation.ts:3` → `type QAStatus = 'success' | 'hold' | 'fail'`

| 수정 전 | 수정 후 |
|--------|--------|
| `Pass/Hold/Fail` 3단계 | `Success/Hold/Fail` 3단계 |
| 표에 `Pass` | 표에 `success` (실제 타입 리터럴 반영) |
| hold 조건 설명 불명확 | `pass=false` / `failure_types` 조건 명시 |

---

## frontend/README.md

### 1. 날짜 갱신

`2026-04-14` → `2026-05-07`

### 2. API 클라이언트 — `UPLOAD_BASE` 누락

**근거**: `frontend/src/lib/api.ts:8` → `const UPLOAD_BASE = import.meta.env.VITE_UPLOAD_BASE_URL ?? ''`

업로드 전용 base URL이 `API_BASE`와 분리되어 있었으나 문서화되지 않았음. 두 상수 모두 표로 추가.

### 3. `apiFetch` 공개 여부 오류

**근거**: `frontend/src/lib/api.ts:122` → `async function apiFetch(...)` (export 없음)

| 위치 | 수정 전 | 수정 후 |
|------|--------|--------|
| 공통 래퍼 표 | `apiFetch` 공개 함수로 기술 | 내부 함수로 명시 |
| 디렉토리 주석 | `apiFetch / apiFetchWithRetry 래퍼` | `apiFetchWithRetry 공개 래퍼 + 내부 apiFetch` |

### 4. 파일 규모 표 갱신

기준일 `2026-04-08` → `2026-05-07`

| 파일 | 수정 전 | 수정 후 |
|------|--------|--------|
| `DataStandardizationPanel.tsx` | 581줄 | 606줄 |
| `api.ts` | 270줄 | 300줄 |
| `DashboardOverview.tsx` | 미기재 | 602줄 추가 |

디렉토리 구조 주석 `~581줄` → `~606줄` 함께 수정.

---

## 검토 과정에서 이상 없음 확인한 항목

| 항목 | 확인 방법 |
|------|----------|
| API 엔드포인트 전체 목록 | `ingestion_api.py`, `generation_api.py`, `evaluation_api.py`, `main.py` 라우트 직접 대조 |
| 모델 alias / model_id 매핑 | `config/models.py` 대조 |
| 평가 가중치 (0.05/0.05/0.65/0.25) | `evaluators/pipeline.py:562-566` 대조 |
| 등급 체계 (A+/A/B+/B/C/F) | `evaluators/pipeline.py:569-574` 대조 |
| 인텐트 6종 (fact/purpose/how/condition/comparison/list) | `generators/prompts.py` 대조 |
| 임베딩 모델 (`gemini-embedding-2-preview`, 1536차원) | `ingestion/pipeline.py`, `generators/worker.py` 대조 |
| 평가 workers (google=10, openai=8, anthropic=2) | `evaluators/pipeline.py:25-27` 대조 |
| 생성 workers (google=5, openai=5, anthropic=2) | `generators/worker.py:41-43` 대조 |
| QA 상태 판정 로직 | `evalScoreUtils.ts` 전체 대조 |
| 컴포넌트 Props 정의 | `App.tsx`, 각 패널 컴포넌트 대조 |
| 평가 훅 3종 반환값 | `hooks/` 디렉토리 전체 대조 |
